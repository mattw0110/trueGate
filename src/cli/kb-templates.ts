// AUTOGENERATED by tools/build-kb-templates.mjs — do not edit by hand.
// Source: ~/.truegate/ as of 2026-05-28T23:28:41.950Z
// Re-run `node tools/build-kb-templates.mjs` to refresh.

/**
 * Operator-wide governance knowledge-base templates bundled into the
 * truegate binary. The `truegate kb-init` command writes these files
 * into a target directory.
 *
 * Keys are paths relative to the destination root (e.g. `topics/code-style.md`).
 * Values are the file content verbatim.
 */
export const KB_TEMPLATES: Record<string, string> = {
  "components/buttons.md": "# Buttons\n\nUse the native `<button>` element. The pattern below is what every variant should compose from.\n\n## The shape\n\n```tsx\n<button\n  type=\"button\" // explicit — type=\"submit\" is the default inside <form>\n  onClick={handler}\n  disabled={isLoading}\n  aria-busy={isLoading}\n  className=\"\n    inline-flex items-center gap-2 rounded px-4 py-2\n    bg-blue-600 text-white text-sm font-medium\n    hover:bg-blue-700\n    focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2\n    disabled:opacity-50 disabled:cursor-not-allowed\n  \"\n>\n  {isLoading ? <Spinner /> : <Icon />}\n  Save changes\n</button>\n```\n\n## Variants\n\n| Variant                            | Use                                     |\n| ---------------------------------- | --------------------------------------- |\n| **Primary** (filled, accent color) | One per form / view — the main action   |\n| **Secondary** (outline or muted)   | Alternative actions (Cancel, Back)      |\n| **Destructive** (red filled)       | Delete, archive, irreversible actions   |\n| **Ghost** (no background)          | Tertiary actions in dense UI (toolbars) |\n| **Link** (styled as text link)     | Navigation that looks like text         |\n\nOnly ONE primary button per form / view section.\n\n## Icon buttons\n\nA button with only an icon needs an accessible name:\n\n```tsx\n<button\n  type=\"button\"\n  aria-label=\"Close dialog\" // required — without it, screen readers announce \"button\"\n  onClick={onClose}\n  className=\"rounded p-2 hover:bg-gray-100 focus:ring-2\"\n>\n  <XIcon className=\"h-5 w-5\" aria-hidden=\"true\" />\n</button>\n```\n\nUse `aria-label` (icon-only) OR visible text inside the button. Never both — that double-announces.\n\n## Loading state\n\n```tsx\n<button type=\"submit\" disabled={pending} aria-busy={pending}>\n  {pending ? (\n    <>\n      <Spinner aria-hidden=\"true\" />\n      <span>Saving…</span>\n    </>\n  ) : (\n    \"Save\"\n  )}\n</button>\n```\n\n- `disabled={true}` prevents double-submission\n- `aria-busy={true}` tells assistive tech the button is in a transient state\n- Show changed text — \"Saving…\" not just a spinner alone — for screen reader users\n\n## Touch / pointer targets\n\n**24×24 CSS px minimum** (WCAG 2.2 SC 2.5.8). 44×44 (iOS HIG) is the better target. Default to `px-4 py-2` for text buttons, `p-2` for icon-only.\n\n## Don't do\n\n- `<div role=\"button\" onClick={...}>` — now you need to handle Enter/Space yourself, focus, ARIA state. Just use `<button>`\n- `<a href=\"#\" onClick={...}>` — that's a fake link. Use `<button>` for actions, `<a>` for navigation\n- `onClick={() => router.push('/x')}` on a button — that should be `<Link href=\"/x\">`\n- Remove `outline` without a replacement focus style\n- Disable a button without explaining why (tooltip, helper text, or aria-disabled with focusable=true + explanation)\n- 12px tall buttons people can't tap reliably\n- Use color alone to signal danger — pair red with an icon or word (\"Delete\")\n\n## See also\n\n- [./forms.md](./forms.md) — submit buttons specifically\n- [../topics/accessibility.md](../topics/accessibility.md) — WCAG 2.2 target size, focus\n",
  "components/data-display.md": "# Data display\n\nTables, lists, empty states, loading skeletons. Patterns that handle the realistic states data lives in.\n\n## The four states every data view needs\n\n| State         | What                                                                  |\n| ------------- | --------------------------------------------------------------------- |\n| **Loading**   | Server hasn't responded yet — show a skeleton or progress indicator   |\n| **Empty**     | Query succeeded but no rows — show an empty state, not a blank screen |\n| **Error**     | Query failed — show a retry path, not a stack trace                   |\n| **Populated** | Normal happy path                                                     |\n\nIf you ship only the populated state, you ship bugs.\n\n## Tables\n\n```tsx\n<div className=\"overflow-x-auto\">\n  <table className=\"min-w-full divide-y divide-gray-200\">\n    <caption className=\"sr-only\">List of recent orders</caption>\n    <thead className=\"bg-gray-50\">\n      <tr>\n        <th scope=\"col\" className=\"px-4 py-2 text-left text-sm font-medium\">\n          Order\n        </th>\n        <th scope=\"col\" className=\"px-4 py-2 text-left text-sm font-medium\">\n          Customer\n        </th>\n        <th scope=\"col\" className=\"px-4 py-2 text-right text-sm font-medium\">\n          Total\n        </th>\n      </tr>\n    </thead>\n    <tbody className=\"divide-y divide-gray-100\">\n      {orders.map((o) => (\n        <tr key={o.id} className=\"hover:bg-gray-50\">\n          <td className=\"px-4 py-2\">\n            <Link href={`/orders/${o.id}`}>#{o.id}</Link>\n          </td>\n          <td className=\"px-4 py-2\">{o.customer}</td>\n          <td className=\"px-4 py-2 text-right tabular-nums\">\n            {formatMoney(o.total)}\n          </td>\n        </tr>\n      ))}\n    </tbody>\n  </table>\n</div>\n```\n\nRules:\n\n- **`<caption>` (`sr-only`)** — screen reader users hear what the table is BEFORE they navigate it\n- **`scope=\"col\"` / `scope=\"row\"`** on `<th>` — associates headers with cells\n- **Wrap in `overflow-x-auto`** — mobile/small screens scroll horizontally rather than reflowing\n- **`tabular-nums`** on numeric columns — digits align vertically\n- **Sort state**: `aria-sort=\"ascending\" | \"descending\" | \"none\"` on the sorted column's `<th>`\n- **Don't use tables for layout.** `<table>` is for data; use grid/flex for layout\n- For complex tables (column resize, virtualization, row selection): use TanStack Table\n\n## Lists\n\n```tsx\n<ul role=\"list\" className=\"divide-y divide-gray-200\">\n  {items.map((item) => (\n    <li key={item.id} className=\"py-3 flex justify-between\">\n      <span>{item.name}</span>\n      <span className=\"text-gray-500\">{item.detail}</span>\n    </li>\n  ))}\n</ul>\n```\n\n`role=\"list\"` is needed when CSS removes list bullets (`list-style: none`) — Safari de-lists styled `<ul>` for screen readers. Set explicitly.\n\n## Empty states\n\n```tsx\n{\n  orders.length === 0 ? (\n    <div className=\"text-center py-12\">\n      <ShoppingBagIcon\n        className=\"mx-auto h-12 w-12 text-gray-400\"\n        aria-hidden=\"true\"\n      />\n      <h3 className=\"mt-2 text-sm font-medium\">No orders yet</h3>\n      <p className=\"mt-1 text-sm text-gray-500\">\n        Your first order will appear here.\n      </p>\n      <div className=\"mt-6\">\n        <Link\n          href=\"/products\"\n          className=\"rounded bg-blue-600 px-4 py-2 text-white\"\n        >\n          Browse products\n        </Link>\n      </div>\n    </div>\n  ) : (\n    <OrderTable orders={orders} />\n  );\n}\n```\n\nA good empty state:\n\n- Says what's missing\n- Says WHY it might be missing (first-time? filtered out?)\n- Offers a next action\n\n## Loading skeletons\n\nSkeleton screens beat spinners for perceived performance — they hint at the shape of what's coming.\n\n```tsx\n<div\n  className=\"animate-pulse space-y-4\"\n  aria-busy=\"true\"\n  aria-label=\"Loading orders\"\n>\n  {[...Array(5)].map((_, i) => (\n    <div key={i} className=\"h-12 rounded bg-gray-200\" />\n  ))}\n</div>\n```\n\n- `aria-busy=\"true\"` + `aria-label` so screen readers announce loading\n- Reserve space the loaded content will fill — prevents CLS\n- Match the shape, not just \"a grey rectangle\"\n- Skip skeletons if loading is < 200ms — flash is worse than no skeleton\n\n## Error states\n\n```tsx\n<div role=\"alert\" className=\"rounded border border-red-300 bg-red-50 p-4\">\n  <h3 className=\"text-sm font-medium text-red-800\">Couldn't load orders</h3>\n  <p className=\"mt-1 text-sm text-red-700\">{error.message}</p>\n  <button\n    onClick={retry}\n    className=\"mt-3 rounded bg-red-600 px-3 py-1 text-sm text-white\"\n  >\n    Try again\n  </button>\n</div>\n```\n\n- `role=\"alert\"` — announced immediately\n- Show user-meaningful text, not raw error.message in production (use a friendly message + a copyable error ID)\n- Always offer a retry path or alternative\n\n## Pagination UI\n\nFor cursor-based pagination (see [../topics/backend.md](../topics/backend.md)):\n\n```tsx\n<nav aria-label=\"Pagination\" className=\"flex justify-between mt-4\">\n  <button disabled={!prevCursor} onClick={() => loadPage(prevCursor)}>\n    ← Previous\n  </button>\n  <button disabled={!nextCursor} onClick={() => loadPage(nextCursor)}>\n    Next →\n  </button>\n</nav>\n```\n\nFor offset-based with numbered pages: number the current page in an `aria-current=\"page\"` link.\n\n## Don't do\n\n- Show a blank screen for empty data\n- Spin forever on a fetch that already errored\n- Use a `<table>` for two-column \"layouts\"\n- Show the raw error.message from a server crash (leaks stack traces / internals)\n- Skeleton with the wrong shape (raises expectations the real content can't meet)\n- Truncate text without a way to see the full value (tooltip on hover OR detail page)\n\n## See also\n\n- [../topics/frontend.md](../topics/frontend.md) — server-side data fetching patterns\n- [../topics/accessibility.md](../topics/accessibility.md) — table semantics\n- [../patterns/data-fetching.md](../patterns/data-fetching.md) — handling the four states in code\n",
  "components/feedback.md": "# Feedback — toasts, alerts, modals, status\n\nPatterns for talking back to the user.\n\n## Inline status\n\nFor results that belong with the form/action that produced them:\n\n```tsx\n{\n  state.message && (\n    <p\n      role={state.error ? \"alert\" : \"status\"}\n      aria-live={state.error ? \"assertive\" : \"polite\"}\n      className={state.error ? \"text-red-600\" : \"text-green-600\"}\n    >\n      {state.message}\n    </p>\n  );\n}\n```\n\n- `role=\"alert\"` / `aria-live=\"assertive\"` for errors — interrupts speech\n- `role=\"status\"` / `aria-live=\"polite\"` for non-urgent updates (\"Saved\")\n- Place near the action that produced it, not floating at the page top\n\n## Toasts\n\nFor transient confirmations not tied to a specific UI region.\n\nUse a library: [sonner](https://sonner.emilkowal.ski/) is the current React default. shadcn/ui uses it.\n\n```tsx\nimport { toast } from \"sonner\";\n\ntoast.success(\"Order placed\");\ntoast.error(\"Couldn't save changes\", {\n  description: \"Network error. Retry?\",\n  action: { label: \"Retry\", onClick: retry },\n});\n```\n\nToasts should:\n\n- Auto-dismiss after 4-6 seconds for non-critical messages\n- NEVER auto-dismiss errors that require action\n- Be paused on hover and on focus\n- Be reachable by keyboard (Tab to the toast, Enter to act)\n- Use `aria-live` regions correctly (sonner does this)\n\nDon't use toasts for:\n\n- Validation errors on a specific form field (use inline)\n- Critical errors that require user choice (use a dialog)\n- The result of a navigation (the destination IS the feedback)\n\n## Alerts (banner-style)\n\nFor warnings or info that stays visible:\n\n```tsx\n<div role=\"alert\" className=\"rounded border border-yellow-300 bg-yellow-50 p-4\">\n  <div className=\"flex items-start gap-3\">\n    <ExclamationIcon className=\"h-5 w-5 text-yellow-600\" aria-hidden=\"true\" />\n    <div className=\"flex-1\">\n      <h3 className=\"text-sm font-medium text-yellow-800\">\n        Subscription expiring\n      </h3>\n      <p className=\"mt-1 text-sm text-yellow-700\">\n        Your subscription ends in 3 days.\n      </p>\n    </div>\n    <button\n      type=\"button\"\n      aria-label=\"Dismiss\"\n      onClick={onDismiss}\n      className=\"text-yellow-600 hover:text-yellow-800\"\n    >\n      <XIcon className=\"h-5 w-5\" aria-hidden=\"true\" />\n    </button>\n  </div>\n</div>\n```\n\n- `role=\"alert\"` ONLY when the alert appears dynamically (not on initial render of a page)\n- For always-visible banners, no role needed — just semantic markup\n- Color + icon + text — never color alone\n- Dismissible if appropriate; sticky if action is required\n\n## Modals / dialogs\n\n**Use Radix Dialog or shadcn/ui Dialog.** Modal a11y is hard:\n\n- Focus traps inside while open\n- Returns focus to the trigger when closed\n- Closes on Escape\n- Backdrop click closes (sometimes; depends on UX)\n- `role=\"dialog\"` + `aria-labelledby` + `aria-modal=\"true\"`\n- Scroll lock on body while open\n- Stack management for nested dialogs\n\nLibrary handles all of this. Don't roll your own:\n\n```tsx\n<Dialog open={open} onOpenChange={setOpen}>\n  <Dialog.Trigger asChild>\n    <button>Delete account</button>\n  </Dialog.Trigger>\n  <Dialog.Content>\n    <Dialog.Title>Delete account?</Dialog.Title>\n    <Dialog.Description>This action cannot be undone.</Dialog.Description>\n    <Dialog.Footer>\n      <Dialog.Close asChild>\n        <button>Cancel</button>\n      </Dialog.Close>\n      <button onClick={deleteAccount} className=\"bg-red-600 text-white\">\n        Delete\n      </button>\n    </Dialog.Footer>\n  </Dialog.Content>\n</Dialog>\n```\n\n## Confirmations vs immediate actions\n\n- **Destructive irreversible** action (delete account, archive contract): confirmation modal required\n- **Destructive reversible** action (soft delete with undo): toast with \"Undo\" action; no modal\n- **Non-destructive** action: just do it\n\nExcessive confirmation modals train users to dismiss them — they become noise. Use them only when the action is genuinely hard to reverse.\n\n## Loading feedback\n\n- < 1s: no indicator needed (most users won't notice)\n- 1-3s: spinner or skeleton (see [data-display.md](./data-display.md))\n- 3-10s: progress indicator with status text (\"Uploading… 3 of 5 files\")\n- > 10s: don't make the user wait — queue the work and notify on completion\n\n## Optimistic UI\n\nFor actions where the result is highly likely to succeed (like, follow, mark-as-read), update the UI immediately and reconcile on the server response.\n\n```tsx\n\"use client\";\nimport { useOptimistic } from \"react\";\n\nexport function LikeButton({ count, liked, onLike }) {\n  const [optimistic, addOptimistic] = useOptimistic(\n    { count, liked },\n    (state, delta) => ({ count: state.count + delta, liked: !state.liked }),\n  );\n  return (\n    <button\n      onClick={async () => {\n        addOptimistic(optimistic.liked ? -1 : 1);\n        await onLike();\n      }}\n    >\n      ♥ {optimistic.count}\n    </button>\n  );\n}\n```\n\nIf the server rejects, React rolls back the optimistic state on the next render.\n\n## Don't do\n\n- Use `alert()` / `confirm()` / `prompt()` browser dialogs in production UI\n- Toasts that auto-dismiss errors users need to act on\n- Spinners with no text — screen readers can't see them\n- Block all interaction with a backdrop for a 200ms request\n- Backdrop modals you can't close with Escape\n- Toasts in the top-right that obscure nav links\n\n## See also\n\n- [../topics/accessibility.md](../topics/accessibility.md) — live regions, role=alert vs status\n- [./forms.md](./forms.md) — inline form errors\n- [sonner](https://sonner.emilkowal.ski/), [Radix Dialog](https://www.radix-ui.com/primitives/docs/components/dialog)\n",
  "components/forms.md": "# Forms\n\nAccessible, progressive-enhanced form pattern. Works without JS via Server Actions; gets nicer with JS.\n\n## The shape\n\n```tsx\n// app/contact/page.tsx — Server Component\nimport { submitContact } from \"./actions\";\nimport { SubmitButton } from \"./submit-button\";\n\nexport default function ContactPage() {\n  return (\n    <form action={submitContact} className=\"max-w-md space-y-4\">\n      <div>\n        <label htmlFor=\"email\" className=\"block text-sm font-medium\">\n          Email <span aria-label=\"required\">*</span>\n        </label>\n        <input\n          id=\"email\"\n          name=\"email\"\n          type=\"email\"\n          required\n          autoComplete=\"email\"\n          aria-describedby=\"email-error\"\n          className=\"mt-1 block w-full rounded border-gray-300 focus:border-blue-500 focus:ring-blue-500\"\n        />\n        <p\n          id=\"email-error\"\n          role=\"alert\"\n          aria-live=\"polite\"\n          className=\"mt-1 text-sm text-red-600 empty:hidden\"\n        />\n      </div>\n\n      <div>\n        <label htmlFor=\"message\" className=\"block text-sm font-medium\">\n          Message\n        </label>\n        <textarea\n          id=\"message\"\n          name=\"message\"\n          required\n          rows={4}\n          aria-describedby=\"message-error\"\n          className=\"mt-1 block w-full rounded border-gray-300 focus:border-blue-500 focus:ring-blue-500\"\n        />\n        <p\n          id=\"message-error\"\n          role=\"alert\"\n          aria-live=\"polite\"\n          className=\"mt-1 text-sm text-red-600 empty:hidden\"\n        />\n      </div>\n\n      <SubmitButton />\n    </form>\n  );\n}\n```\n\n```tsx\n// app/contact/submit-button.tsx — Client Component (uses useFormStatus)\n\"use client\";\nimport { useFormStatus } from \"react-dom\";\n\nexport function SubmitButton() {\n  const { pending } = useFormStatus();\n  return (\n    <button\n      type=\"submit\"\n      disabled={pending}\n      aria-busy={pending}\n      className=\"rounded bg-blue-600 px-4 py-2 text-white disabled:opacity-50\"\n    >\n      {pending ? \"Sending…\" : \"Send message\"}\n    </button>\n  );\n}\n```\n\n```tsx\n// app/contact/actions.ts — Server Action with Zod validation\n\"use server\";\nimport { z } from \"zod\";\nimport { redirect } from \"next/navigation\";\n\nconst ContactSchema = z.object({\n  email: z.string().email(\"Enter a valid email\"),\n  message: z.string().min(10, \"Message must be at least 10 characters\"),\n});\n\nexport async function submitContact(formData: FormData) {\n  const parsed = ContactSchema.safeParse({\n    email: formData.get(\"email\"),\n    message: formData.get(\"message\"),\n  });\n\n  if (!parsed.success) {\n    // Returning the errors to the page requires useActionState — see below.\n    return { errors: parsed.error.flatten().fieldErrors };\n  }\n\n  await sendEmail(parsed.data);\n  redirect(\"/contact/thanks\");\n}\n```\n\n## Returning errors to the form (useActionState)\n\nWhen you need server-validated errors to render inline:\n\n```tsx\n// app/contact/page.tsx — switch to Client Component to use useActionState\n\"use client\";\nimport { useActionState } from \"react\";\nimport { submitContact, type ContactState } from \"./actions\";\n\nconst initialState: ContactState = { errors: {}, message: null };\n\nexport default function ContactPage() {\n  const [state, formAction] = useActionState(submitContact, initialState);\n  return (\n    <form action={formAction} className=\"max-w-md space-y-4\">\n      <div>\n        <label htmlFor=\"email\">\n          Email <span aria-label=\"required\">*</span>\n        </label>\n        <input\n          id=\"email\"\n          name=\"email\"\n          type=\"email\"\n          required\n          autoComplete=\"email\"\n          aria-invalid={Boolean(state.errors.email)}\n          aria-describedby={state.errors.email ? \"email-error\" : undefined}\n        />\n        {state.errors.email && (\n          <p id=\"email-error\" role=\"alert\" className=\"text-sm text-red-600\">\n            {state.errors.email[0]}\n          </p>\n        )}\n      </div>\n      {/* ...message field, submit button */}\n    </form>\n  );\n}\n```\n\n## Accessibility checklist\n\n- `<label htmlFor>` on every field. **No placeholder-as-label.**\n- `autoComplete` attribute — `email`, `current-password`, `new-password`, `name`, `tel`, etc.\n- `required` + visible `*` indicator. Screen readers announce required automatically.\n- `aria-invalid=\"true\"` when there's an error.\n- `aria-describedby` linking the input to its error message.\n- Error region with `role=\"alert\"` OR `aria-live=\"polite\"` — announces error when it appears.\n- Visible focus state (Tailwind: `focus:ring-2 focus:ring-blue-500`).\n- Don't disable submit while invalid — let users try; surface the error.\n- Group related fields with `<fieldset><legend>` (e.g. checkbox groups, radio groups).\n- For multi-step forms, announce step changes via `aria-live`.\n\n## With React Hook Form (richer client validation)\n\n```tsx\n\"use client\";\nimport { useForm } from \"react-hook-form\";\nimport { zodResolver } from \"@hookform/resolvers/zod\";\nimport { ContactSchema } from \"./schemas\";\n\nexport function ContactForm() {\n  const {\n    register,\n    handleSubmit,\n    formState: { errors, isSubmitting },\n  } = useForm({ resolver: zodResolver(ContactSchema) });\n\n  return (\n    <form\n      onSubmit={handleSubmit(async (data) => {\n        /* call Server Action */\n      })}\n    >\n      <input\n        {...register(\"email\")}\n        type=\"email\"\n        aria-invalid={!!errors.email}\n        aria-describedby={errors.email ? \"email-error\" : undefined}\n      />\n      {errors.email && (\n        <p id=\"email-error\" role=\"alert\">\n          {errors.email.message}\n        </p>\n      )}\n      <button type=\"submit\" disabled={isSubmitting}>\n        {isSubmitting ? \"Sending…\" : \"Send\"}\n      </button>\n    </form>\n  );\n}\n```\n\n## Don't do\n\n- Use `placeholder` as the label (disappears on focus, unreliable for screen readers)\n- Disable the submit button while invalid (users can't trigger error display)\n- Validate only on blur — let the user submit and announce errors\n- Color-only error indication (red border alone is invisible to ~8% of men)\n- Submit form on Enter inside a `<textarea>` (browsers don't, but custom handlers sometimes do — preserve the default)\n- Wipe what the user typed on a validation failure (preserve via `defaultValue` + state)\n- Trigger a redirect inside `onSubmit` before the Server Action resolves (race condition)\n\n## See also\n\n- [../topics/frontend.md](../topics/frontend.md) — Server Actions, useActionState, useFormStatus details\n- [../topics/accessibility.md](../topics/accessibility.md) — WCAG 2.2 form criteria\n- [./buttons.md](./buttons.md) — submit button states\n",
  "components/navigation.md": "# Navigation\n\nNav bars, breadcrumbs, tabs, command palette. Patterns that keep users oriented.\n\n## Top-level navigation\n\n```tsx\n<header className=\"border-b\">\n  <nav\n    aria-label=\"Primary\"\n    className=\"mx-auto flex max-w-6xl items-center justify-between p-4\"\n  >\n    <Link href=\"/\" aria-label=\"Home\" className=\"text-lg font-semibold\">\n      <Logo />\n    </Link>\n    <ul className=\"flex gap-6\">\n      <li>\n        <Link\n          href=\"/products\"\n          aria-current={pathname === \"/products\" ? \"page\" : undefined}\n        >\n          Products\n        </Link>\n      </li>\n      <li>\n        <Link\n          href=\"/pricing\"\n          aria-current={pathname === \"/pricing\" ? \"page\" : undefined}\n        >\n          Pricing\n        </Link>\n      </li>\n      <li>\n        <Link\n          href=\"/docs\"\n          aria-current={pathname?.startsWith(\"/docs\") ? \"page\" : undefined}\n        >\n          Docs\n        </Link>\n      </li>\n    </ul>\n    <UserMenu />\n  </nav>\n</header>\n```\n\nRules:\n\n- **`<nav aria-label=\"...\">`** — each `<nav>` needs a unique label (Primary, Footer, Pagination)\n- **`aria-current=\"page\"`** on the link to the current page — screen readers announce it\n- **`<ul>` of links** — landmark + list structure\n- **One `<header>` per page** with the primary nav inside\n- **Skip-link** as the FIRST focusable element on every page: `<a href=\"#main\" className=\"sr-only focus:not-sr-only\">Skip to content</a>`\n\n## Mobile nav (disclosure)\n\n```tsx\n\"use client\";\nimport { useState } from \"react\";\n\nexport function MobileNav() {\n  const [open, setOpen] = useState(false);\n  return (\n    <>\n      <button\n        type=\"button\"\n        aria-expanded={open}\n        aria-controls=\"mobile-menu\"\n        onClick={() => setOpen((v) => !v)}\n        className=\"md:hidden p-2\"\n      >\n        <span className=\"sr-only\">{open ? \"Close menu\" : \"Open menu\"}</span>\n        {open ? <XIcon /> : <MenuIcon />}\n      </button>\n      <div\n        id=\"mobile-menu\"\n        hidden={!open}\n        className=\"absolute inset-x-0 top-16 border-b bg-white p-4\"\n      >\n        {/* nav links */}\n      </div>\n    </>\n  );\n}\n```\n\n- `aria-expanded` mirrors open state\n- `aria-controls` references the menu's `id`\n- Visible button label (sr-only text) changes with state\n- Close on Escape + click-outside (omitted for brevity — implement both)\n\n## Breadcrumbs\n\n```tsx\n<nav aria-label=\"Breadcrumb\" className=\"text-sm\">\n  <ol className=\"flex gap-2\">\n    <li>\n      <Link href=\"/\">Home</Link>\n    </li>\n    <li aria-hidden=\"true\">/</li>\n    <li>\n      <Link href=\"/products\">Products</Link>\n    </li>\n    <li aria-hidden=\"true\">/</li>\n    <li aria-current=\"page\">{product.name}</li>\n  </ol>\n</nav>\n```\n\n- Ordered list — order matters\n- Separator `/` hidden from screen readers (it's decorative)\n- Last item is `aria-current=\"page\"` and NOT a link\n\n## Tabs\n\nTabs are deceptively hard to do right. Use Radix UI Tabs or shadcn/ui's Tabs component unless you have a strong reason not to.\n\nIf you must roll your own, follow the [WAI-ARIA Tabs Pattern](https://www.w3.org/WAI/ARIA/apg/patterns/tabs/):\n\n```tsx\n<div>\n  <div role=\"tablist\" aria-label=\"Account settings\">\n    <button\n      role=\"tab\"\n      id=\"tab-profile\"\n      aria-controls=\"panel-profile\"\n      aria-selected={tab === \"profile\"}\n      onClick={() => setTab(\"profile\")}\n    >\n      Profile\n    </button>\n    <button\n      role=\"tab\"\n      id=\"tab-billing\"\n      aria-controls=\"panel-billing\"\n      aria-selected={tab === \"billing\"}\n      onClick={() => setTab(\"billing\")}\n    >\n      Billing\n    </button>\n  </div>\n\n  <div\n    role=\"tabpanel\"\n    id=\"panel-profile\"\n    aria-labelledby=\"tab-profile\"\n    hidden={tab !== \"profile\"}\n  >\n    {/* profile content */}\n  </div>\n  <div\n    role=\"tabpanel\"\n    id=\"panel-billing\"\n    aria-labelledby=\"tab-billing\"\n    hidden={tab !== \"billing\"}\n  >\n    {/* billing content */}\n  </div>\n</div>\n```\n\nPlus keyboard handlers: Arrow Left/Right between tabs, Home/End to first/last. Easier with a library.\n\n## Command palette (Cmd+K)\n\nFor developer-facing apps: use [`cmdk`](https://github.com/pacocoursey/cmdk) (the library shadcn/ui wraps). Don't roll your own — focus management + filtering + keyboard navigation is too easy to get wrong.\n\n## Don't do\n\n- `<div onClick={() => router.push('/x')}>` — that's a fake link. Use `<Link href=\"/x\">`\n- `<a>` without `href` (not focusable)\n- Hide focus styles on nav links\n- Auto-open a dropdown on hover only (keyboard / touch users can't reach it)\n- Forget `aria-current` — the user loses track of where they are\n- Build tabs without arrow-key navigation\n- Stick the user in an infinite breadcrumb when they came from a different surface\n\n## See also\n\n- [WAI-ARIA Authoring Practices Guide](https://www.w3.org/WAI/ARIA/apg/)\n- [shadcn/ui](https://ui.shadcn.com/) — pre-built accessible primitives\n- [../topics/accessibility.md](../topics/accessibility.md)\n",
  "governance.md": "---\ntitle: Operator-wide governance (matt@patientize.com)\nnotes: |\n  This file is the INDEX. It applies to every project trueGate serves and is\n  always injected. The detailed topic files live in ~/.truegate/topics/*.md\n  and the AI should read them on demand when relevant — they are kept under\n  ~200 lines each so they fit in context without crowding out task work.\n---\n\n# Operator Governance\n\nThis is the **index** of operator-wide policy that applies to every project. Per-project files (`CLAUDE.md`, `.truegate/governance.md`, `AGENTS.md`, `.cursor/rules/*.mdc`) extend these rules — they cannot remove them.\n\n> **For AI agents reading this:** when you're about to do work in an area covered below, read the linked topic file first. Each file is short (≤200 lines) and dense; the round trip is cheap and prevents you from regressing to outdated patterns.\n\n## Conflict resolution (priority, highest first)\n\ntrueGate has NO per-project artifacts. Projects own their own conventions; the operator-wide layer below defers to them.\n\n1. **Security floor** — non-negotiable (no destructive shell, no leaked credentials, no TLS bypass, no `DROP TABLE`). trueGate's response validator blocks these regardless of any source.\n2. **Project documentation** (highest authoritative layer for the project):\n   - `CLAUDE.md`\n   - `AGENTS.md`\n   - `.cursor/rules/*.mdc`\n   - any other `docs/ai/INSTRUCTIONS.md` the project maintains\n3. **This operator-wide guidance** — applies when the project is silent. Defers to the project on every conflict; the AI is instructed to follow the project and note the conflict in its response.\n\n## Cross-agent consistency\n\nMultiple AI coding tools may be active at once (Claude Code, Cursor, Continue.dev, Codex, GitHub Copilot, Cody). They MUST behave consistently.\n\n- Canonical agent instructions live in each repo's `docs/ai/INSTRUCTIONS.md` when present\n- Tool-native adapters are kept in sync with that canonical file:\n  - Cursor: `.cursor/rules/*.mdc`\n  - Continue: `.continue/rules/*.md`\n  - Copilot: `.github/copilot-instructions.md` and `.github/instructions/*.instructions.md`\n  - Codex: `AGENTS.md` (root + nested where needed)\n- On conflict between tools' instructions, defer to `docs/ai/INSTRUCTIONS.md`. If that file doesn't exist, defer to `CLAUDE.md`. This operator-wide file is the **lowest** priority — it applies only when the project is silent.\n\n## Topic index — read on demand\n\n| Topic                                                | Read when                                                                          |\n| ---------------------------------------------------- | ---------------------------------------------------------------------------------- |\n| [topics/code-style.md](./topics/code-style.md)       | Writing or refactoring any code — naming, function shape, error handling, comments |\n| [topics/security.md](./topics/security.md)           | Auth, secrets, input validation, anything touching user data                       |\n| [topics/accessibility.md](./topics/accessibility.md) | Generating any UI markup or interaction                                            |\n| [topics/performance.md](./topics/performance.md)     | Anything user-perceivable — Core Web Vitals + API latency budgets                  |\n| [topics/frontend.md](./topics/frontend.md)           | React 19, Next.js 15, Tailwind v4, forms, state, images                            |\n| [topics/backend.md](./topics/backend.md)             | HTTP APIs, idempotency, observability, OAuth 2.1, rate limiting                    |\n| [topics/database.md](./topics/database.md)           | Schema, indexes, migrations (expand/contract), connection pooling                  |\n| [topics/testing.md](./topics/testing.md)             | What to test, fixtures, flaky-test diagnosis                                       |\n| [topics/architecture.md](./topics/architecture.md)   | SOLID, layering, hexagonal, DDD-lite, ADRs                                         |\n| [topics/documentation.md](./topics/documentation.md) | READMEs, ADRs, when comments are appropriate                                       |\n| [topics/git-and-pr.md](./topics/git-and-pr.md)       | Commits (Conventional Commits), branches, PR descriptions                          |\n\n### Component recipes — drop-in accessible UI patterns\n\n| File                                                       | Use when                                                              |\n| ---------------------------------------------------------- | --------------------------------------------------------------------- |\n| [components/forms.md](./components/forms.md)               | Building a form (validation, error display, Server Action submission) |\n| [components/buttons.md](./components/buttons.md)           | Any button — variants, icon buttons, loading states                   |\n| [components/data-display.md](./components/data-display.md) | Tables, lists, empty states, loading skeletons                        |\n| [components/navigation.md](./components/navigation.md)     | Nav bars, breadcrumbs, tabs, command palette                          |\n| [components/feedback.md](./components/feedback.md)         | Toasts, alerts, modals, status messages                               |\n\n### Design pattern templates\n\n| File                                                           | Use when                                                      |\n| -------------------------------------------------------------- | ------------------------------------------------------------- |\n| [patterns/error-handling.md](./patterns/error-handling.md)     | Where to throw, where to catch, structured error responses    |\n| [patterns/data-fetching.md](./patterns/data-fetching.md)       | Server-first fetching, the four UI states, cache invalidation |\n| [patterns/state-management.md](./patterns/state-management.md) | Local vs URL vs server vs context vs store — decision tree    |\n| [patterns/auth-boundaries.md](./patterns/auth-boundaries.md)   | Where AuthN/AuthZ checks belong, defense in depth             |\n\n### External authorities\n\n- [references/README.md](./references/README.md) — curated index of canonical sources (RFCs, MDN, OWASP, framework docs)\n\n## Always (the operator-wide floor)\n\nThese apply everywhere; don't bother re-reading a topic file to confirm them:\n\n- **Never embed credentials, API keys, tokens, or private hostnames** in code, comments, or config files committed to git\n- **Never generate destructive shell commands** (`rm -rf /`, `format c:`, `mkfs`, `dd if=/dev/zero`) — trueGate blocks these, but they should never even be suggested\n- **Never disable TLS verification** (`rejectUnauthorized: false`, `verify=False`, `-k`) in production paths\n- **Authorization checks at the boundary, always.** No early returns \"for performance\" that skip an authz check\n- **Treat all external input as untrusted** — URL params, headers, request bodies, file contents, env vars from third parties\n- **No `eval()`, `new Function()`**, or arbitrary code execution from input\n- **No silent fallbacks** that mask real errors\n\n## Style floor\n\n- **Names**: functions are verbs (`parseConfig`), classes are nouns (`UserStore`), booleans are predicates (`isReady`)\n- **Comments explain WHY, not WHAT.** Code shows what; comments explain hidden constraints\n- **One env-access module per project.** No sprinkling `process.env.X` across the codebase\n- **Reject `any` / `dynamic` / `interface{}`** outside well-justified boundaries\n- See [topics/code-style.md](./topics/code-style.md) for the full set\n\n## Definition of Done (operator-wide minimum)\n\nA change is complete when:\n\n- Types pass (`tsc`, `mypy --strict`, `go build`, equivalents)\n- Tests that cover the change pass\n- No secrets, hardcoded credentials, or private hostnames committed\n- No debug leftovers (`console.log`, `print(...)`, `dump`) unless intentional and routed through the project's logger\n- No new lint or formatter errors\n- Documentation updated when behavior visibly changed\n- Sentrux gate clean if Sentrux is configured in this project\n\n## Communication style (when responding to me)\n\n- Get to the answer in the first sentence. No throat-clearing.\n- One short update at the start of significant tool work, not narration of every step.\n- For one-line commands: just give the command. Don't wrap it in three paragraphs of context.\n- Match my register: technical, direct, no emoji unless I use one first.\n- If I ask for two things, do both. Don't ask which one first unless they conflict.\n\n## When in doubt\n\n1. Check the relevant `topics/*.md` file\n2. Check the project's `CLAUDE.md`\n3. Check `~/.truegate/references/README.md` for the canonical external source\n4. Ask before guessing\n\n— trueGate operator policy v1\n",
  "patterns/auth-boundaries.md": "# Auth boundaries — where checks belong\n\nAuthN (identity) and AuthZ (permission) are different. Both happen at the request boundary. Defense in depth at the data layer.\n\n## The layered shape\n\n```\n┌─ Edge (CDN / load balancer)\n│    DDoS, rate limit per IP, geofence\n│\n├─ Gateway (middleware)\n│    AuthN: verify token → populate `req.user`\n│    Coarse AuthZ: drop if no auth on a protected path\n│\n├─ Route handler\n│    Fine AuthZ: can THIS user perform THIS action on THIS resource?\n│    Throw before reading data\n│\n├─ Service / use case\n│    Trusts the principal — operates on already-validated input\n│\n└─ Data layer\n   Defense in depth: row-level filters by tenant_id / user_id\n   RLS in Postgres if available\n```\n\n## At the gateway — AuthN\n\n```ts\n// middleware/auth.ts\nfastify.addHook(\"preHandler\", async (req, reply) => {\n  const token = req.headers.authorization?.replace(/^Bearer /, \"\");\n  if (!token) return; // route handler decides if anonymous is allowed\n\n  try {\n    const claims = await verifyToken(token);\n    req.user = {\n      id: claims.sub,\n      roles: claims.roles,\n      tenantId: claims.tenant_id,\n    };\n  } catch {\n    return reply.status(401).send(problemDetails({ code: \"invalid_token\" }));\n  }\n});\n```\n\n## At the route — AuthZ\n\n```ts\nfastify.get('/orders/:id', async (req, reply) => {\n  if (!req.user) return reply.status(401).send(...);    // require auth\n\n  const order = await loadOrder(req.params.id);\n  if (!order) return reply.status(404).send(...);\n\n  // Object-level authz — does THIS user own THIS order?\n  if (order.userId !== req.user.id && !req.user.roles.includes('admin')) {\n    return reply.status(403).send(...);  // 403, not 404 — they don't OWN it\n  }\n\n  return reply.send(order);\n});\n```\n\n**Subtle point**: 403 vs 404 leaks information. Some teams prefer 404 for \"you don't have access\" to avoid revealing the resource exists. Pick a policy per project and document it.\n\n## At the data layer — defense in depth\n\nEven with route-level checks, the data layer should filter:\n\n```ts\n// repository\nasync function loadOrder(orderId: string, userId: string) {\n  return db.order.findFirst({\n    where: { id: orderId, userId }, // user can ONLY see their own\n  });\n}\n```\n\nIf the route forgets to check, the repository still doesn't return another user's row. Two layers must both fail for a breach.\n\n## Postgres Row-Level Security\n\n```sql\nALTER TABLE orders ENABLE ROW LEVEL SECURITY;\n\nCREATE POLICY orders_tenant_isolation ON orders\n  FOR ALL\n  USING (tenant_id = current_setting('app.tenant_id')::uuid);\n```\n\n```ts\n// At the start of each request, set the tenant context\nawait db.$executeRaw`SET LOCAL app.tenant_id = ${req.user.tenantId}::text`;\n```\n\nNow the DB itself rejects queries that would leak another tenant's rows. App code STILL filters by tenant_id — RLS is the safety net, not the only check. (Defeats OWASP API1 — Broken Object Level Authorization / BOLA.)\n\n## Role-based vs Attribute-based authorization\n\n- **RBAC** (roles): `admin`, `editor`, `viewer`. Simple, works for most apps.\n- **ABAC** (attributes): rules like \"user can edit a document if `document.team_id IN user.teams AND user.role >= editor`\". Use when permissions depend on data, not just role.\n- **Policy as code**: [Oso](https://www.osohq.com/), [OPA / Rego](https://www.openpolicyagent.org/), [Cedar](https://www.cedarpolicy.com/) — externalize when authz logic gets complex.\n\n## Don't do\n\n- **Skip auth check \"for performance\"** — the #1 cause of vulnerabilities. AuthZ is cheap; data leaks are expensive\n- **Trust IDs from the client without checking ownership** — OWASP API1 BOLA\n- **Authorize in the frontend only** — frontend is UX, not security\n- **Use route-prefix-based \"admin\" check** without verifying the user is an admin (`req.path.startsWith('/admin')` is not a check)\n- **Stale JWTs with no revocation path** — pair short-lived JWTs with refresh-token rotation\n- **Pass `req.user` 5 layers deep** — pass an authorization context or check at the boundary\n- **Return different error codes** that hint at the resource's existence (info leak)\n\n## See also\n\n- [../topics/security.md](../topics/security.md) — OWASP, secrets, TLS\n- [../topics/backend.md](../topics/backend.md) — OAuth 2.1 specifics\n- [../topics/database.md](../topics/database.md) — RLS specifics\n",
  "patterns/data-fetching.md": "# Data fetching\n\nServer-first. Handle the four states. Invalidate explicitly.\n\n## The decision tree\n\n```\nDoes the user own this data and need it on initial render?\n├─ Yes → Server Component, fetch in the component, render\n└─ No\n   ├─ Comes from an interaction (button click, form submit)?\n   │  ├─ Mutation → Server Action + revalidatePath/Tag\n   │  └─ Search/filter → URL state (searchParams) + Server Component\n   └─ Long-polling or push from server?\n      └─ Streaming (Suspense), Server-Sent Events, or WebSocket\n```\n\n## Server Component fetch (default in Next 15)\n\n```tsx\n// app/orders/page.tsx\nexport default async function OrdersPage() {\n  const orders = await fetch(\"https://api.example.com/orders\", {\n    next: { revalidate: 60, tags: [\"orders\"] },\n    headers: { authorization: `Bearer ${await getServiceToken()}` },\n  }).then((r) => r.json());\n\n  return <OrderTable orders={orders} />;\n}\n```\n\n- `next: { revalidate: N }` — ISR; cache for N seconds\n- `cache: 'no-store'` — never cache (per-request)\n- `cache: 'force-cache'` — SSG-like\n- `next: { tags: ['orders'] }` — invalidate via `revalidateTag('orders')` from a Server Action\n\n## Mutation + revalidation\n\n```ts\n// app/orders/actions.ts\n\"use server\";\nimport { revalidateTag, redirect } from \"next/cache\";\n\nexport async function placeOrder(formData: FormData) {\n  const order = await api.placeOrder({\n    productId: formData.get(\"productId\"),\n    quantity: Number(formData.get(\"quantity\")),\n  });\n  revalidateTag(\"orders\"); // refresh all pages tagged 'orders'\n  redirect(`/orders/${order.id}`);\n}\n```\n\n## The four states (every UI must handle)\n\n| State         | Handling                                                                                                         |\n| ------------- | ---------------------------------------------------------------------------------------------------------------- |\n| **Loading**   | `loading.tsx` (route-level) OR `<Suspense fallback={...}>` (component-level)                                     |\n| **Empty**     | After the data resolves, check `data.length === 0` and render an empty state                                     |\n| **Error**     | `error.tsx` (route-level) catches thrown errors. Wrap fetches in try/catch when you want graceful inline failure |\n| **Populated** | The normal render path                                                                                           |\n\nSee [../components/data-display.md](../components/data-display.md) for the markup of each state.\n\n## Client-side fetching (when needed)\n\nUse TanStack Query for genuinely client-driven data — search-as-you-type, real-time dashboards, things behind user interaction:\n\n```tsx\n\"use client\";\nimport { useQuery } from \"@tanstack/react-query\";\n\nexport function SearchResults({ query }: { query: string }) {\n  const { data, isPending, isError, error } = useQuery({\n    queryKey: [\"search\", query],\n    queryFn: () =>\n      fetch(`/api/search?q=${encodeURIComponent(query)}`).then((r) => r.json()),\n    enabled: query.length > 2,\n  });\n\n  if (isPending) return <Skeleton />;\n  if (isError) return <Error message={error.message} />;\n  if (data.length === 0) return <Empty />;\n  return <Results data={data} />;\n}\n```\n\n## Don't do\n\n- **`useEffect` + `fetch`** for initial data — use Server Components\n- **Waterfall fetches** — kick off in parallel: `const [a, b] = await Promise.all([fetchA(), fetchB()])`\n- **Fetch in render without Suspense or caching** — produces a request per render\n- **Store server data in Zustand/Redux** — you'll reinvent caching, dedupe, retries. Use TanStack Query / SWR / RSC\n- **Manual cache invalidation by busting query keys** — use tags + `revalidateTag` (Next.js) or `queryClient.invalidateQueries({ queryKey: [...] })` (TanStack)\n- **Show a loading state for < 200ms requests** — feels worse than no state at all\n- **Hide the error and show \"Something went wrong\"** with no recovery path — always offer Retry\n\n## See also\n\n- [../topics/frontend.md](../topics/frontend.md) — Next.js 15 caching defaults\n- [../topics/backend.md](../topics/backend.md) — pagination, error responses\n- [../components/data-display.md](../components/data-display.md) — UI for the four states\n",
  "patterns/error-handling.md": "# Error handling\n\nThrow early. Catch at boundaries. Structured errors at the wire.\n\n## The shape\n\n```ts\n// 1. Throw with context, deep in the call stack\nasync function loadUser(id: string): Promise<User> {\n  const row = await db.user.findUnique({ where: { id } });\n  if (!row) throw new NotFoundError(\"user\", id);\n  return row;\n}\n\n// 2. Don't catch in the middle — let it propagate\nasync function getOrderDetails(orderId: string) {\n  const order = await loadOrder(orderId);\n  const user = await loadUser(order.userId); // can throw — fine, propagate\n  return { order, user };\n}\n\n// 3. Catch at the boundary (HTTP, CLI, queue consumer)\nfastify.get(\"/orders/:id\", async (req, reply) => {\n  try {\n    const data = await getOrderDetails(req.params.id);\n    return reply.send(data);\n  } catch (err) {\n    if (err instanceof NotFoundError) {\n      return reply.status(404).send(problemDetails(err));\n    }\n    if (err instanceof ValidationError) {\n      return reply.status(422).send(problemDetails(err));\n    }\n    req.log.error({ err, orderId: req.params.id }, \"unexpected error\");\n    return reply.status(500).send(problemDetails(new ServerError()));\n  }\n});\n```\n\n## Custom error types\n\n```ts\nexport class AppError extends Error {\n  constructor(\n    message: string,\n    public readonly code: string,\n    public readonly status: number,\n    public readonly details?: Record<string, unknown>,\n  ) {\n    super(message);\n    this.name = this.constructor.name;\n  }\n}\n\nexport class NotFoundError extends AppError {\n  constructor(resource: string, id: string) {\n    super(`${resource} ${id} not found`, \"not_found\", 404, { resource, id });\n  }\n}\n\nexport class ValidationError extends AppError {\n  constructor(public readonly fieldErrors: Record<string, string[]>) {\n    super(\"Validation failed\", \"validation_error\", 422, { fieldErrors });\n  }\n}\n```\n\n## RFC 9457 Problem Details on the wire\n\n```ts\nfunction problemDetails(err: AppError): ProblemDetailsBody {\n  return {\n    type: `https://api.example.com/problems/${err.code}`,\n    title: err.message,\n    status: err.status,\n    detail: err.details?.detail,\n    instance: requestId(),\n    ...err.details,\n  };\n}\n```\n\nReturn `Content-Type: application/problem+json`. See [topics/backend.md](../topics/backend.md) for the full spec.\n\n## Don't do\n\n- **Empty `catch {}`** — that's a silent bug. If you intentionally ignore, comment why and at minimum log it\n- **Catch to re-throw with no added context** — the original stack is enough\n- **`throw new Error(JSON.stringify(...))`** — use a typed error class, serialize at the boundary\n- **`if (err.message.includes('not found'))`** — don't match on message strings; check `err instanceof` or `err.code`\n- **Use exceptions for control flow** — return a `Result<T, E>` or `[err, data]` tuple if the failure is expected\n- **Hide the real error from logs** — show the user a friendly message; log the full stack\n- **Catch + log + re-throw** at three levels — produces duplicate logs and obscures the real entry point\n\n## See also\n\n- [../topics/code-style.md](../topics/code-style.md) — error-handling section\n- [../topics/backend.md](../topics/backend.md) — RFC 9457 Problem Details\n",
  "patterns/state-management.md": "# State management — decision tree\n\nMost \"global state\" is actually server state. Most \"complex state\" is actually URL state. Reach for a client store last.\n\n## Decision tree\n\n```\nWhere does this state live?\n\n1. Shareable URL? (filters, tabs, pagination)\n   → URL state (searchParams). Server Components can read it directly.\n\n2. Comes from your server? (lists, details, user info)\n   → Server Components for initial render + Server Actions for mutations.\n   → OR TanStack Query / SWR if it changes client-side or you need optimistic updates.\n   → Either way, NOT in a global client store.\n\n3. UI-only, scoped to one component / small tree?\n   → useState / useReducer.\n\n4. UI-only, shared across many components, LOW-frequency updates? (theme, locale, current user)\n   → React Context.\n\n5. UI-only, shared, HIGH-frequency or non-trivial shape? (canvas state, editor, multi-step wizard)\n   → Zustand (store-shape) or Jotai (atomic/derived).\n\n6. Form state?\n   → React Hook Form for client validation, Server Action for submission.\n```\n\n## What goes wrong when state lives in the wrong place\n\n| Symptom                                                | Likely cause                                             | Fix                                                                     |\n| ------------------------------------------------------ | -------------------------------------------------------- | ----------------------------------------------------------------------- |\n| URL doesn't reflect what's on screen                   | UI state that should be in URL                           | Move filters / tabs / pagination to `searchParams`                      |\n| Refresh loses state user expected to keep              | Same — UI-only when should be URL                        | Same                                                                    |\n| \"Why doesn't this update?\" — stale data after mutation | Server data cached in Zustand                            | Move to TanStack Query with proper invalidation, or RSC + revalidateTag |\n| Cascade re-renders, \"everything re-renders\"            | Context with too many consumers + high-frequency updates | Split contexts by update frequency, or move to Zustand/Jotai            |\n| State logic spread across 10 files                     | useState scattered everywhere                            | Hoist to a useReducer in a parent, or extract a feature-level hook      |\n\n## Examples\n\n### URL state (Next 15)\n\n```tsx\n// app/products/page.tsx — Server Component\ntype Props = { searchParams: Promise<{ category?: string; sort?: string }> };\n\nexport default async function ProductsPage({ searchParams }: Props) {\n  const params = await searchParams;\n  const products = await loadProducts({\n    category: params.category,\n    sort: params.sort,\n  });\n  return <ProductGrid products={products} currentCategory={params.category} />;\n}\n```\n\n```tsx\n// app/products/filters.tsx — Client Component to UPDATE the URL\n\"use client\";\nimport { useRouter, useSearchParams } from \"next/navigation\";\n\nexport function CategoryFilter() {\n  const params = useSearchParams();\n  const router = useRouter();\n  const setCategory = (c: string) => {\n    const next = new URLSearchParams(params);\n    next.set(\"category\", c);\n    router.replace(`?${next}`, { scroll: false });\n  };\n  return (\n    <select onChange={(e) => setCategory(e.target.value)}>{/* ... */}</select>\n  );\n}\n```\n\n### Local state (useReducer for non-trivial)\n\n```tsx\n\"use client\";\nimport { useReducer } from \"react\";\n\ntype State = {\n  step: number;\n  data: Partial<FormData>;\n  errors: Record<string, string>;\n};\ntype Action =\n  | { type: \"next\" }\n  | { type: \"prev\" }\n  | { type: \"set\"; field: string; value: string };\n\nfunction reducer(state: State, action: Action): State {\n  switch (action.type) {\n    case \"next\":\n      return { ...state, step: state.step + 1 };\n    case \"prev\":\n      return { ...state, step: state.step - 1 };\n    case \"set\":\n      return {\n        ...state,\n        data: { ...state.data, [action.field]: action.value },\n      };\n  }\n}\n\nexport function MultiStepForm() {\n  const [state, dispatch] = useReducer(reducer, {\n    step: 0,\n    data: {},\n    errors: {},\n  });\n  // ...\n}\n```\n\n### Zustand (client store, last resort)\n\n```ts\n// stores/editor.ts\nimport { create } from \"zustand\";\n\ntype EditorStore = {\n  zoom: number;\n  setZoom: (z: number) => void;\n  reset: () => void;\n};\n\nexport const useEditor = create<EditorStore>((set) => ({\n  zoom: 1,\n  setZoom: (z) => set({ zoom: z }),\n  reset: () => set({ zoom: 1 }),\n}));\n```\n\n```tsx\nconst zoom = useEditor((s) => s.zoom); // subscribes to ONLY zoom — no re-render on other changes\n```\n\n## Don't do\n\n- **Put server data in Redux/Zustand** — you'll reinvent caching, dedupe, retries, invalidation\n- **Use Context for high-frequency updates** — cascade re-renders all consumers\n- **`useState` for state that should be URL** — refresh breaks the user's flow\n- **Add Redux to a small app** — too much ceremony for the value\n- **Make every component a Client Component \"just in case\"** — you lose RSC benefits\n\n## See also\n\n- [../topics/frontend.md](../topics/frontend.md) — state management decision order, what's overused\n- [./data-fetching.md](./data-fetching.md) — server state via TanStack Query / RSC\n",
  "references/README.md": "# References — external authorities\n\nCurated index of canonical sources. When in doubt, link here rather than guessing.\n\n## Web standards\n\n- [W3C WCAG 2.2](https://www.w3.org/TR/WCAG22/) — accessibility (current)\n- [W3C ARIA Authoring Practices Guide](https://www.w3.org/WAI/ARIA/apg/) — patterns for ARIA usage\n- [MDN Web Docs](https://developer.mozilla.org/) — HTML / CSS / JS / Web APIs reference\n- [web.dev — Core Web Vitals](https://web.dev/articles/vitals) — LCP, INP, CLS thresholds\n- [W3C Trace Context](https://www.w3.org/TR/trace-context/) — distributed tracing headers\n\n## RFCs (HTTP & friends)\n\n- [RFC 9110 — HTTP Semantics](https://www.rfc-editor.org/rfc/rfc9110.html) — methods, status codes, headers\n- [RFC 9111 — HTTP Caching](https://www.rfc-editor.org/rfc/rfc9111.html)\n- [RFC 9112 — HTTP/1.1](https://www.rfc-editor.org/rfc/rfc9112.html)\n- [RFC 9457 — Problem Details](https://www.rfc-editor.org/rfc/rfc9457.html) — error response shape (obsoletes 7807)\n- [RFC 9562 — UUIDs](https://www.rfc-editor.org/rfc/rfc9562.html) — UUIDv7 (time-ordered)\n- [RFC 8288 — Web Linking](https://www.rfc-editor.org/rfc/rfc8288.html) — `Link` headers\n- [RFC 6585 — Additional HTTP Status Codes](https://www.rfc-editor.org/rfc/rfc6585.html) — 429 and friends\n- [RFC 7636 — PKCE](https://www.rfc-editor.org/rfc/rfc7636.html) — OAuth proof key for code exchange\n- [draft-ietf-oauth-v2-1](https://datatracker.ietf.org/doc/draft-ietf-oauth-v2-1/) — OAuth 2.1 consolidation\n- [RFC 9700 — OAuth 2.0 Security BCP](https://www.rfc-editor.org/rfc/rfc9700.html)\n- [draft-ietf-httpapi-ratelimit-headers](https://datatracker.ietf.org/doc/draft-ietf-httpapi-ratelimit-headers/) — `RateLimit:` headers\n\n## Security\n\n- [OWASP Top 10 (2021)](https://owasp.org/www-project-top-ten/) — web app risks\n- [OWASP API Security Top 10 (2023)](https://owasp.org/API-Security/) — API-specific risks\n- [OWASP ASVS](https://owasp.org/www-project-application-security-verification-standard/) — verification standard\n- [OWASP Cheat Sheets](https://cheatsheetseries.owasp.org/) — defense recipes\n- [Secrets Management Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Secrets_Management_Cheat_Sheet.html)\n\n## API design\n\n- [Microsoft REST API Guidelines](https://github.com/microsoft/api-guidelines) — pragmatic, public-facing\n- [Google AIP](https://google.aip.dev/) — API Improvement Proposals\n- [JSON:API spec](https://jsonapi.org/) — response shape conventions\n- [Stripe API design](https://docs.stripe.com/api) — gold-standard idempotency + errors\n\n## Architecture & design\n\n- [martinfowler.com](https://martinfowler.com/) — the canonical reference for most architecture topics\n- [martinfowler.com/bliki/](https://martinfowler.com/bliki/) — short essays on patterns\n- [Robert Martin — Clean Coder Blog](https://blog.cleancoder.com/) — SOLID, Clean Architecture\n- [Alistair Cockburn — Hexagonal Architecture](https://alistair.cockburn.us/hexagonal-architecture/)\n- [DDD Reference (Eric Evans)](https://www.domainlanguage.com/ddd/reference/)\n- [adr.github.io](https://adr.github.io/) — Architecture Decision Records resources\n- [Documenting Architecture Decisions — Nygard](https://cognitect.com/blog/2011/11/15/documenting-architecture-decisions)\n- [12factor.net](https://12factor.net/) — Twelve-Factor App methodology\n\n## Frontend frameworks\n\n- [React docs](https://react.dev/) — React 19 reference\n- [Next.js docs](https://nextjs.org/docs) — App Router + Server Components\n- [Tailwind CSS docs](https://tailwindcss.com/docs) — v4\n- [shadcn/ui](https://ui.shadcn.com/) — pre-built accessible primitives\n- [Radix UI Primitives](https://www.radix-ui.com/primitives) — unstyled accessible components\n\n## Testing\n\n- [kentcdodds.com — Testing Trophy](https://kentcdodds.com/blog/the-testing-trophy-and-testing-classifications)\n- [Testing Library](https://testing-library.com/) — query-by-role docs\n- [Playwright docs](https://playwright.dev/) — E2E\n- [Vitest docs](https://vitest.dev/) — unit/integration test runner\n- [MSW (Mock Service Worker)](https://mswjs.io/) — network mocking\n- [testcontainers](https://testcontainers.com/) — real services in tests\n- [Test Data Builders — Nat Pryce](http://www.natpryce.com/articles/000714.html)\n- [Mocks Aren't Stubs — Fowler](https://martinfowler.com/articles/mocksArentStubs.html)\n\n## Databases & migrations\n\n- [PostgreSQL docs](https://www.postgresql.org/docs/current/)\n- [Use the Index, Luke](https://use-the-index-luke.com/) — practical indexing guide\n- [Strong Migrations](https://github.com/ankane/strong_migrations) — catalog of dangerous migrations\n- [gh-ost (MySQL online schema changes)](https://github.com/github/gh-ost)\n- [pt-online-schema-change](https://docs.percona.com/percona-toolkit/pt-online-schema-change.html)\n\n## Observability\n\n- [OpenTelemetry docs](https://opentelemetry.io/docs/)\n- [Prometheus naming conventions](https://prometheus.io/docs/practices/naming/)\n- [RED method](https://grafana.com/blog/2018/08/02/the-red-method-how-to-instrument-your-services/)\n- [USE method (Brendan Gregg)](https://www.brendangregg.com/usemethod.html)\n\n## Conventions\n\n- [Conventional Commits](https://www.conventionalcommits.org/)\n- [Keep A Changelog](https://keepachangelog.com/)\n- [Semantic Versioning](https://semver.org/)\n\n## Performance\n\n- [web.dev — Core Web Vitals](https://web.dev/articles/vitals)\n- [web.dev — INP](https://web.dev/articles/inp)\n- [Lighthouse docs](https://developer.chrome.com/docs/lighthouse/)\n\n## When this index goes stale\n\n- Specs (RFCs, WCAG, OWASP) update at known intervals — re-check yearly\n- Framework docs are versioned — confirm the link points at the LTS/stable version\n- \"Best practice\" essays from individuals (Fowler, Kent C. Dodds) have stable URLs but newer takes may exist on the same site\n",
  "topics/accessibility.md": "# Accessibility — WCAG 2.2 AA\n\nThe current standard is **WCAG 2.2** (published October 2023). AA is the practical baseline for production sites; AAA is for specific high-bar contexts (government, healthcare).\n\nFull spec: [w3.org/TR/WCAG22](https://www.w3.org/TR/WCAG22/)\n\n## What WCAG 2.2 added vs 2.1\n\nSix new success criteria worth knowing:\n\n| Criterion                            | What it requires                                                                                  |\n| ------------------------------------ | ------------------------------------------------------------------------------------------------- |\n| 2.4.11 Focus Not Obscured (AA)       | Focused element can't be hidden behind sticky headers/toolbars                                    |\n| 2.4.12 Focus Not Obscured (AAA)      | Stricter version of above                                                                         |\n| 2.4.13 Focus Appearance (AAA)        | Focus indicator has a minimum visible area and contrast                                           |\n| 2.5.7 Dragging Movements (AA)        | Anything you can drag must also have a click/tap alternative                                      |\n| 2.5.8 Target Size (AA)               | Interactive targets are ≥ 24×24 CSS px (with limited exceptions)                                  |\n| 3.2.6 Consistent Help (A)            | Help mechanisms (chat, contact, FAQ) appear in the same relative location                         |\n| 3.3.7 Redundant Entry (A)            | Don't ask the user to re-enter info they already provided                                         |\n| 3.3.8 Accessible Authentication (AA) | No cognitive function tests like remembering or transcribing codes — unless an alternative exists |\n\nSource: [w3.org/TR/WCAG22/#new-features-in-wcag-2-2](https://www.w3.org/TR/WCAG22/#new-features-in-wcag-2-2)\n\n## The non-negotiables (catches 80% of failures)\n\n### Semantic HTML\n\n- **Use the right element.** `<button>` for actions, `<a href>` for navigation, `<input type=\"...\">` for inputs. Native elements are keyboard-accessible and announce correctly to screen readers for free.\n- **`<div role=\"button\">` is a tax.** You now have to handle keyboard activation (Enter + Space), focus, and ARIA state yourself. Only do it when no native element fits.\n- **Landmarks**: `<header>`, `<nav>`, `<main>`, `<aside>`, `<footer>`. Screen-reader users navigate by landmark. One `<main>` per page.\n- **Headings in order.** `<h1>` once per page; never skip levels (`<h2>` → `<h4>`).\n\n### Keyboard\n\n- **Every interactive element reachable by Tab.**\n- **Visible focus indicator.** Don't `outline: none` without a replacement.\n- **Focus order matches visual order.** No reverse-order DOM tricks.\n- **Escape closes modals.** Tab cycles inside open modals (focus trap).\n- **Skip links**: a \"Skip to content\" link as the first focusable element on every page.\n\n### Forms\n\n- **Every input has a label.** `<label for=\"email\">` + `<input id=\"email\">` OR `<label>Email <input></label>`. Never rely on `placeholder` as a label.\n- **Error messages announced.** `aria-describedby` linking input to error text; the error region uses `role=\"alert\"` or `aria-live=\"polite\"`.\n- **`required` + `aria-required=\"true\"`** for required fields. Server-side validation is the source of truth.\n- **`autocomplete` attributes**: `autocomplete=\"email\"`, `autocomplete=\"current-password\"`, etc. Helps password managers AND assistive tech.\n\n### Images\n\n- **`alt` on every `<img>`.** Empty (`alt=\"\"`) for decorative; descriptive for content.\n- **SVG: `<svg role=\"img\" aria-label=\"...\">`.** Or `aria-hidden=\"true\"` if decorative.\n- **Don't put text in images.** Real text is selectable, scalable, translatable.\n\n### Color and contrast\n\n- **Text contrast ≥ 4.5:1** for normal text, ≥ 3:1 for large text (18pt+ or 14pt+ bold).\n- **UI contrast ≥ 3:1** for interactive elements vs adjacent colors.\n- **Color is never the only carrier of information.** Red errors must ALSO have an icon or text label.\n- **Tools**: [WebAIM Contrast Checker](https://webaim.org/resources/contrastchecker/), Lighthouse, axe DevTools.\n\n### Touch / pointer targets\n\n- **24×24 CSS px minimum** for any interactive target (WCAG 2.2). 44×44 is the iOS HIG recommendation and a better aim.\n- **Spacing between targets** matters as much as size — accidental taps are a real failure mode.\n\n### Motion and animation\n\n- **Respect `prefers-reduced-motion`.** Skip non-essential transitions for users who request it.\n- **No auto-playing motion > 5 seconds** without pause/stop controls.\n- **No flashing > 3 times per second.** Triggers seizures.\n\n### Live regions\n\n- **`aria-live=\"polite\"`** for status messages (\"Saved\", \"Item added to cart\").\n- **`role=\"alert\"`** for important interruptions (errors, warnings). Use sparingly.\n- **`aria-busy`** while a region is loading.\n\n## Testing\n\n- **Automated**: axe-core (via Playwright, Jest-axe), Lighthouse CI. Catches ~40% of issues — necessary but not sufficient.\n- **Manual keyboard pass**: tab through every page; can you complete every flow?\n- **Screen reader pass**: NVDA (free, Windows) or VoiceOver (Mac). Even 15 min reveals problems.\n- **Zoom to 200%**: layout should still work, no horizontal scroll.\n\n## Don't do\n\n- Putting `role=\"button\"` on a `<div>` and forgetting keyboard handlers\n- `outline: none` with no replacement focus style\n- `<a>` without `href` (it's not focusable)\n- Skipping heading levels for styling reasons (style with CSS, not the wrong tag)\n- Carousels with auto-advance and no pause control\n- Tooltips that only appear on hover (must also work on focus)\n- Color-only error indication\n- Tiny click targets next to each other\n\n## See also\n\n- [frontend.md](./frontend.md) — React/Next a11y specifics\n- [components/forms.md](../components/forms.md) — accessible form patterns\n- [references/README.md](../references/README.md) — links to W3C ARIA Authoring Practices, MDN ARIA, axe-core\n",
  "topics/architecture.md": "# Architecture\n\nPatterns that scale, with honest \"when not to use\" guidance.\n\n## SOLID\n\nRobert C. Martin's five OO design principles. Modern restatement + what each ACTUALLY catches:\n\n- **S — Single Responsibility**: a module has one reason to change, defined by one actor/stakeholder. Catches god classes that mix persistence + formatting + business rules — when marketing's copy change forces redeploy of the billing engine.\n- **O — Open/Closed**: open for extension, closed for modification. Catches long `switch`/`if-elseif` chains on type tags that grow with every new variant. Refactor to polymorphism / strategy.\n- **L — Liskov Substitution**: subtypes honor the supertype's contract (preconditions no stronger, postconditions no weaker). Catches `Square extends Rectangle`, `ReadOnlyList extends List` throwing on `add()`.\n- **I — Interface Segregation**: clients don't depend on methods they don't use. Catches fat interfaces forcing test doubles to stub 15 unused methods. Split by _consumer_, not by _implementer_.\n- **D — Dependency Inversion**: depend on abstractions, not concretions; high-level policy doesn't import low-level detail. Catches domain code that `import { pg }` directly.\n\nSources: [cleancoder.com — SOLID Relevance](https://blog.cleancoder.com/uncle-bob/2020/10/18/Solid-Relevance.html); Martin, _Clean Architecture_ (2017).\n\n## Hexagonal / Ports and Adapters\n\nCockburn, 2005. Application core exposes **ports** (interfaces defined by the core's needs); **adapters** translate between ports and the outside world (HTTP, DB, queue, CLI). Driving adapters (UI, tests) on one side; driven adapters (DB, APIs) on the other.\n\n**Why it's the most testable architecture:** the core has zero outbound concrete dependencies. Tests drive it through driving ports with in-memory adapters for driven ports — no HTTP, no DB, no framework mocks.\n\n**Reach for it when:** non-trivial domain logic; multiple delivery channels (HTTP + CLI + cron); long-lived codebase; might need to swap infra.\n\n**Overkill when:** CRUD-over-HTTP with no domain rules; one delivery channel; throwaway script; < ~2k LOC.\n\nSource: [alistair.cockburn.us/hexagonal-architecture](https://alistair.cockburn.us/hexagonal-architecture/).\n\n## Layering\n\nTypical layers, outermost to innermost:\n\n1. **Web / Delivery** — HTTP handlers, controllers, serialization\n2. **Application** — use cases, orchestration, transactions\n3. **Domain** — entities, value objects, domain services, invariants\n4. **Infrastructure** — DB clients, message brokers, external API clients\n\n**The Dependency Rule (Clean Architecture):** source-code dependencies point inward only. Domain must NEVER import application, web, or infrastructure. Application must NEVER import web. Infra implements interfaces _owned by_ domain/application.\n\nWhat never to import upward: framework types in domain (no `FastifyRequest` in an entity), ORM classes in application (use a repository port), DTOs in domain.\n\nSource: Martin, _Clean Architecture_ (2017) Ch. 22 · [cleancoder.com — The Clean Architecture](https://blog.cleancoder.com/uncle-bob/2012/08/13/the-clean-architecture.html).\n\n## DDD-lite (Domain-Driven Design)\n\nEric Evans, _Domain-Driven Design_ (2003); Vaughn Vernon, _Implementing DDD_ (2013).\n\n- **Ubiquitous language** — code names match what domain experts say. Catches translation bugs at boundaries.\n- **Bounded context** — an explicit boundary inside which a model is consistent. Two contexts can use the same word (\"Order\" in billing vs fulfillment) with different shapes — keep them separate.\n- **Aggregate** — a cluster of entities with one root that enforces invariants; transactions never cross aggregate boundaries.\n- **DDD-lite (practical default):** adopt the _vocabulary_ without the full tactical machinery (events bus, CQRS, event sourcing). Full DDD pays off only with rich, contested domain logic and ≥3 distinct subdomains.\n\nSources: [domainlanguage.com/ddd/reference](https://www.domainlanguage.com/ddd/reference/) · [martinfowler.com/bliki/BoundedContext](https://martinfowler.com/bliki/BoundedContext.html).\n\n## CQRS\n\nGreg Young, ~2010. Separate the model that writes state from the model that reads it.\n\n- **Justified:** reads and writes have wildly different shapes/scales; read load >> write load; reporting/search needs differ from transactional needs.\n- **Not:** simple CRUD; one team; one DB. Plain repositories are fine and cheaper. CQRS doubles your models and adds eventual-consistency to the UX.\n\nSource: [martinfowler.com/bliki/CQRS](https://martinfowler.com/bliki/CQRS.html).\n\n## Event sourcing\n\nPersist state as an append-only log of domain events; current state is a fold over events.\n\n- **Justified:** regulatory audit log is a hard requirement; temporal queries (\"what did we know on date X?\"); collaborative domains needing precise causality (trading, accounting).\n- **Footgun:** anything else. Schema evolution of events is brutal; \"fix the data\" becomes an engineering project. **Most teams should not use event sourcing — use an audit table.**\n\nSource: [martinfowler.com/eaaDev/EventSourcing](https://martinfowler.com/eaaDev/EventSourcing.html).\n\n## Monolith first\n\nFowler's position: start with a single deployable. Microservices add operational tax (network, observability, data consistency, deploy choreography) that's only worth paying once boundaries are _known_. You can't draw correct service boundaries before you understand the domain.\n\n- **Default:** well-modularized monolith.\n- **Extract a service** only on concrete pressure: independent scaling, team autonomy at >50 engineers, runtime/language mismatch.\n\nSources: [martinfowler.com/bliki/MonolithFirst](https://martinfowler.com/bliki/MonolithFirst.html) · [martinfowler.com/bliki/MicroservicePremium](https://martinfowler.com/bliki/MicroservicePremium.html).\n\n## Architecture Decision Records (ADRs)\n\nMichael Nygard, 2011. One short markdown file per architecturally significant decision, checked into the repo.\n\n**Template:**\n\n- **Title** — e.g. `0007-use-postgres-for-primary-store.md`\n- **Status** — proposed | accepted | deprecated | superseded by ADR-NNNN\n- **Context** — forces at play, constraints, what's true now\n- **Decision** — what we will do, in active voice\n- **Consequences** — what becomes easier, harder, what we're betting\n\nNumbered sequentially, immutable once accepted (supersede with a new ADR; don't edit).\n\nSources: [cognitect.com — Documenting Architecture Decisions](https://cognitect.com/blog/2011/11/15/documenting-architecture-decisions) · [adr.github.io](https://adr.github.io/).\n\nSee [documentation.md](./documentation.md) for example ADR template.\n\n## Dependency injection\n\n- **Manual constructor DI** — pass collaborators as constructor args. In TS/Python this covers ~95% of apps. **Composition root** (one place that builds the object graph) at the entry point (`main.ts`, server factory, CLI bootstrap).\n- **DI containers** (InversifyJS, tsyringe, NestJS DI, FastAPI's `Depends`) add value when: large graph with mixed lifecycles (singleton + request-scoped + transient), decorator-driven AOP, framework imposes one.\n- **Why usually unnecessary in TS/Python:** no compile-time wiring problem to solve; ~30 lines of factory code beats a runtime reflection container in clarity and stack traces.\n\nSource: [martinfowler.com/articles/injection](https://martinfowler.com/articles/injection.html).\n\n## Module boundaries\n\n- **Folder-by-type** (`controllers/`, `services/`, `models/`): fine at <5k LOC and one bounded context. Breaks down once a feature spans 4 folders.\n- **Folder-by-feature** (`users/`, `billing/`, `governance/` each containing controller/service/model): scales. Co-locates code that changes together.\n- **Rule of thumb:** small + solo → by-type. Medium/large or multi-team → by-feature, with a shared `core/` for cross-cutting primitives. Internal layering still applies within a feature folder.\n\nSource: [feature-sliced.design](https://feature-sliced.design/); _Clean Architecture_ Ch. 27.\n\n## See also\n\n- [testing.md](./testing.md) — testing strategy\n- [backend.md](./backend.md) — applied to HTTP services\n- [documentation.md](./documentation.md) — ADR template\n- [references/README.md](../references/README.md) — Fowler's bliki, Clean Architecture, DDD Reference\n",
  "topics/backend.md": "# Backend (HTTP APIs, observability, data access)\n\nResearch-backed reference for backend code, current as of 2025/26.\n\n## HTTP API design\n\n**Default to REST for resource-oriented CRUD. GraphQL for client-driven shape selection. JSON-RPC for action-oriented non-resource RPC.**\n\n- Resources are **plural nouns**: `/users`, `/orders/{id}/line-items`. Verbs belong in the HTTP method.\n- HTTP methods: `GET` (safe + idempotent), `POST` (create / non-idempotent action), `PUT` (full replace, idempotent), `PATCH` (partial, idempotent if keyed), `DELETE` (idempotent). [RFC 9110 §9](https://www.rfc-editor.org/rfc/rfc9110.html#name-method-definitions)\n- Status codes that matter: `200`, `201` + `Location`, `202` (async), `204`, `301/308`, `400`, `401`, `403`, `404`, `409`, `410`, `412`, `415`, `422`, `429`, `500`, `503`.\n- **Version in URL prefix** (`/v1/...`) for public APIs. Header versioning hurts cacheability and debuggability.\n- ETags + conditional requests (`If-Match`, `If-None-Match`) for optimistic concurrency.\n\n**When NOT REST:** GraphQL when shapes vary heavily and federation is needed (cost: HTTP caching is hard, N+1 default, field-level auth required). JSON-RPC for internal action verbs. Avoid both as a public API surface unless you're sure.\n\nSources: [Microsoft REST Guidelines](https://github.com/microsoft/api-guidelines) · [Google AIP](https://google.aip.dev/) · [RFC 9110](https://www.rfc-editor.org/rfc/rfc9110.html)\n\n## Idempotency\n\n**Make any retriable mutation idempotent.** \"Exactly-once over a network\" is a myth — the achievable property is \"at-least-once delivery with idempotent processing.\"\n\n- `GET`, `PUT`, `DELETE` are idempotent by spec. `POST` and `PATCH` are not — use an **idempotency key**.\n- **Stripe pattern**: client sends `Idempotency-Key: <uuid-v4>` header. Server stores `(key, request-fingerprint, response)` for **24h** per tenant. On retry: same fingerprint → replay stored response; different fingerprint → `409`; in-flight → `409` or block.\n- Store the **response body and status**, not just \"seen this key\" — the client must get the same answer.\n- Inside the service, propagate idempotency keys to downstream side-effects (queue publishes, third-party charges).\n\nSources: [Stripe idempotency](https://docs.stripe.com/api/idempotent_requests) · [Stripe engineering: idempotency](https://stripe.com/blog/idempotency) · [RFC 9110 §9.2.2](https://www.rfc-editor.org/rfc/rfc9110.html#name-idempotent-methods)\n\n## Error responses — RFC 9457 Problem Details\n\n**Use Problem Details (RFC 9457). It supersedes RFC 7807.** Return `Content-Type: application/problem+json`.\n\n```json\n{\n  \"type\": \"https://api.example.com/problems/insufficient-funds\",\n  \"title\": \"Insufficient funds\",\n  \"status\": 422,\n  \"detail\": \"Account 4242 has balance 10.00; required 25.00.\",\n  \"instance\": \"/transfers/abc123\",\n  \"trace_id\": \"01HXYZ...\"\n}\n```\n\nMembers: `type` (stable URI per problem class), `title` (short summary), `status` (mirrors HTTP), `detail` (specific to this occurrence), `instance` (often a request ID). Extensions encouraged: `errors[]` (per-field validation), `trace_id`, `retry_after`.\n\nSource: [RFC 9457](https://www.rfc-editor.org/rfc/rfc9457.html)\n\n## Pagination\n\n**Cursor-based > offset/limit at scale.**\n\n- Offset/limit: O(n) DB cost (skip-scan), inconsistent results when rows shift, cache-unfriendly.\n- Cursor (keyset): opaque token encoding last seen sort key. DB scans only forward from an index — O(page size).\n- Response shape:\n  ```json\n  { \"data\": [...], \"has_more\": true, \"next_cursor\": \"eyJpZCI6MTIzfQ\" }\n  ```\n- Cursors must be **opaque** (base64 the underlying key). Don't leak ordering.\n- Use the `Link` header with `rel=\"next\"|\"prev\"|\"first\"|\"last\"` for HATEOAS discovery. ([RFC 8288](https://www.rfc-editor.org/rfc/rfc8288.html))\n- Sort key must be **unique and stable** (`(created_at, id)`) — ties cause skipped/duplicated rows.\n\nSource: [Slack engineering — evolving API pagination](https://slack.engineering/evolving-api-pagination-at-slack/)\n\n## Authentication / authorization\n\n**OAuth 2.1 with PKCE for every flow.** [draft-ietf-oauth-v2-1](https://datatracker.ietf.org/doc/draft-ietf-oauth-v2-1/) · [RFC 9700 Security BCP](https://www.rfc-editor.org/rfc/rfc9700.html) · [RFC 7636 PKCE](https://www.rfc-editor.org/rfc/rfc7636.html)\n\nChanges vs OAuth 2.0:\n\n- PKCE required for every authorization code flow.\n- Implicit grant removed.\n- Resource Owner Password Credentials grant removed.\n- Redirect URIs must be exact match (no wildcards).\n- Refresh tokens for public clients must be sender-constrained or rotated.\n- Bearer tokens in query strings forbidden.\n\n**JWT vs opaque tokens:**\n\n| Aspect     | JWT                               | Opaque                   |\n| ---------- | --------------------------------- | ------------------------ |\n| Validation | Local signature verify (fast)     | Introspection call       |\n| Revocation | Hard (wait for expiry / denylist) | Immediate at auth server |\n| Size       | Larger                            | Small                    |\n\nDefault: **short-lived JWT access tokens (≤15 min) + rotating opaque refresh tokens.**\n\n**Where authorization belongs:**\n\n- AuthN at the edge — verify token, populate principal.\n- AuthZ at the **request boundary** of each service — check `(subject, action, resource)` before touching data.\n- Re-check ownership inside the data layer with row-level filters — never trust the ID came from a trusted client.\n\nSee [topics/security.md](./security.md) for the full security floor.\n\n## Rate limiting\n\n- **Token bucket** for user-facing APIs (capacity C, refill r/s). Allows bursts up to C, sustained rate r.\n- **Leaky bucket** for smoothing downstream load.\n- **Sliding window** (counter or log) for production — Redis sorted set or rolling counter.\n\nOn limit hit:\n\n- `429 Too Many Requests` + `Retry-After: <seconds>`\n- `RateLimit` headers ([draft-ietf-httpapi-ratelimit-headers](https://datatracker.ietf.org/doc/draft-ietf-httpapi-ratelimit-headers/)): `RateLimit: limit=100, remaining=42, reset=30`\n\nLayer defenses: CDN per-IP → gateway per-API-key → application per-tenant + per-endpoint → cost-weighted (a search call \"costs\" 10 tokens).\n\n## Observability\n\n**OpenTelemetry is the default.** One SDK, three signals (traces / metrics / logs), one wire format (OTLP), any backend.\n\n- **Traces** with W3C [`traceparent`](https://www.w3.org/TR/trace-context/) propagation across every boundary (HTTP, gRPC, queues).\n- **Structured logs** as JSON: `timestamp` (ISO 8601 UTC), `level`, `message`, `service.name`, `trace_id`, `span_id`. Never log PII, tokens, or full bodies.\n- **RED method** for request-handling: **R**ate, **E**rrors, **D**uration (histogram, expose p50/p95/p99). [grafana.com — RED](https://grafana.com/blog/2018/08/02/the-red-method-how-to-instrument-your-services/)\n- **USE method** for resources: Utilization, Saturation, Errors. [brendangregg.com — USE](https://www.brendangregg.com/usemethod.html)\n- **Prometheus naming**: snake_case, suffix the unit (`http_request_duration_seconds`, `db_connections_open`). Counters end in `_total`. Avoid high-cardinality labels.\n\nSources: [opentelemetry.io](https://opentelemetry.io/docs/) · [prometheus.io/practices/naming](https://prometheus.io/docs/practices/naming/)\n\n## Twelve-Factor — what's still relevant\n\n[12factor.net](https://12factor.net/) — most still applies. Modern additions not in the original:\n\n- **Observability** as a first-class concern (traces + metrics + logs)\n- **API contracts as code** (OpenAPI / protobuf checked in)\n- **Secrets** via a manager (AWS Secrets Manager, Vault), not raw env vars\n- **Health endpoints**: `/livez`, `/readyz`, `/healthz` distinct\n- Stateful workloads (DBs, stream processors) are legitimate — just don't keep session state in app memory\n\n## Database access\n\n- **Raw SQL** for hot paths, complex joins, reporting. ORMs hide cost and N+1 by default.\n- **ORM** for boring CRUD (Prisma, Drizzle, SQLAlchemy, Ecto). Always log generated SQL in dev.\n- **Repository pattern**: useful when a query is reused or sources might swap. Otherwise it's ceremony.\n- **Detect N+1** in tests: assert query count per request, fail build on regression. Eager-load (`include`/`JOIN`) by default for known access patterns.\n- **Prepared statements always.** Every mainstream driver does this automatically with parameterized queries. **Never string-concatenate SQL.**\n- **Pool sizing**: per app process small (5–20). PgBouncer (transaction pooling) in front of Postgres at many instances. Total backend connections ≪ (cores × 2 + spindles).\n- **Read replicas**: route explicitly — don't rely on driver \"auto\" routing; it loses correctness on read-after-write.\n- **Transactions**: keep short. **Never hold a transaction across an external HTTP call.**\n\nSee [topics/database.md](./database.md) for schema and migration patterns.\n\n## Background work\n\nIf a request triggers work the user doesn't need to wait for, **enqueue it.**\n\nQueue choices:\n\n- **Redis Streams / BullMQ / RQ** — small/medium scale, low ops cost\n- **SQS** — managed, simple, AWS\n- **RabbitMQ** — complex routing, fanout, priorities\n- **Kafka / Redpanda** — event streams, replay, fan-out (not really a job queue)\n- **Postgres-as-a-queue** (`SELECT FOR UPDATE SKIP LOCKED`) — fine to thousands/sec, one fewer system\n\nConsumer rules:\n\n- **Idempotent** — every message will be delivered ≥1 time. Dedup with message ID or business key.\n- **Visibility timeout** > worst-case processing time. Heartbeat long jobs.\n- **Exponential backoff + jitter**: `delay = min(cap, base * 2^attempt) * random(0.5, 1.5)`. [AWS — backoff and jitter](https://aws.amazon.com/blogs/architecture/exponential-backoff-and-jitter/)\n- **Dead-letter queue** after N attempts. Alert on DLQ depth.\n- **Poison message isolation** — one bad message must not block the queue.\n- **Ordering**: don't rely on it unless guaranteed (SQS FIFO, Kafka partition). Design messages to be commutative.\n\n## OWASP API Security Top 10 (2023)\n\n[owasp.org/API-Security/](https://owasp.org/API-Security/editions/2023/en/0x00-header/)\n\n| ID    | Risk                                            | Defense                                                                     |\n| ----- | ----------------------------------------------- | --------------------------------------------------------------------------- |\n| API1  | Broken Object Level Authorization (BOLA)        | Authz check on every object access; never trust client IDs                  |\n| API2  | Broken Authentication                           | OAuth 2.1; no rolled-your-own tokens; rate-limit auth endpoints             |\n| API3  | Broken Object Property Level Authorization      | Whitelist fields on read AND write (no `Object.assign(user, req.body)`)     |\n| API4  | Unrestricted Resource Consumption               | Rate limits, payload size caps, query complexity limits, timeouts           |\n| API5  | Broken Function Level Authorization             | Authz per route, deny by default                                            |\n| API6  | Unrestricted Access to Sensitive Business Flows | Detect bot/abuse on signup, checkout, password reset                        |\n| API7  | SSRF                                            | Allowlist outbound URLs; block link-local + metadata IPs                    |\n| API8  | Security Misconfiguration                       | Hardened defaults, security headers (HSTS, CSP), no debug endpoints in prod |\n| API9  | Improper Inventory Management                   | Catalog every API + version; retire old versions deliberately               |\n| API10 | Unsafe Consumption of APIs                      | Validate third-party responses; treat as untrusted input                    |\n\n## See also\n\n- [security.md](./security.md) — TLS, secrets, authn/authz floor\n- [database.md](./database.md) — schema, migrations, queries\n- [performance.md](./performance.md) — caching, pooling, perf budgets\n- [testing.md](./testing.md) — integration test boundaries\n- [references/README.md](../references/README.md) — RFCs, OWASP, OpenTelemetry\n",
  "topics/code-style.md": "# Code style (universal)\n\nLanguage-agnostic conventions. Per-language specifics belong in each repo's `CLAUDE.md`.\n\n## Naming\n\n- **Functions are verbs** that describe an action: `parseConfig`, `loadUser`, `compileTemplate`. Not `config`, `user`, `template`.\n- **Classes / types are nouns** for things: `UserStore`, `RetryPolicy`, `OrderId`. Not `UserStoreManager` (Manager is a smell).\n- **Booleans are predicates**: `isReady`, `hasPermission`, `canEdit`, `shouldRetry`. Never `flag`, `status`, `state` for a boolean.\n- **Constants in `SCREAMING_SNAKE_CASE`** only for true compile-time constants. Runtime config values get normal names.\n- **Avoid abbreviations** unless they're industry-canonical (`HTTP`, `URL`, `UUID`, `SQL`). `usr`, `cfg`, `mgr`, `svc` are smells.\n- **No Hungarian-style prefixes** (`strName`, `iCount`). Types document themselves.\n- **Plural for collections, singular for scalars**: `users` vs `user`. Never `userList`.\n\n## Function shape\n\n- **One job per function.** If you can't name it without \"and\", split it.\n- **Length**: ~30 lines is a soft cap. Past 50, you're hiding structure.\n- **Parameter count**: 3 positional max. Beyond that, take an options object.\n- **Return early.** Nested if-else is a code smell when the body is more than 5 lines.\n- **Pure where possible.** A function that takes inputs and returns outputs without side effects is the easiest to test.\n\n## Error handling\n\n- **Handle at boundaries.** HTTP layer, CLI entry, public API. NOT inside business logic.\n- **Don't swallow errors.** Empty `catch {}` blocks are bugs. If you intentionally ignore an error, comment why.\n- **Throw early.** Validate inputs at the function boundary; assume validity inside.\n- **Specific over generic.** `throw new ValidationError(\"email missing\")` > `throw new Error(\"bad input\")`.\n- **Don't use exceptions for control flow.** Return a `Result` / `Either` if the failure is expected.\n- **Re-throw with context.** When catching to re-throw, attach what you were doing: `throw new Error('parsing /etc/foo.yaml', { cause: err })`.\n\n## Comments\n\n- **Explain WHY, not WHAT.** Code shows what; comments explain hidden constraints, surprising decisions, or the reasoning behind a non-obvious approach.\n- **Delete obvious comments.** `// increment counter` above `counter++` is noise.\n- **No TODOs without owners and dates.** `// TODO(matt, 2026-Q2): replace once API v3 lands` is fine. `// TODO: fix this` is not.\n- **No commented-out code.** Git remembers; delete it.\n- **Doc-comments for public API only.** Internal helpers don't need JSDoc.\n\n## Imports\n\n- **Explicit over implicit.** No wildcard imports in production code (`import *` / `from foo import *`).\n- **Group and sort**: stdlib → third-party → local. Most linters do this automatically.\n- **No circular imports.** If you have one, your boundaries are wrong.\n- **One symbol per line** when there are more than 4 in an import.\n\n## Configuration\n\n- **All env access through one typed module.** Don't sprinkle `process.env.FOO` across the codebase. One file owns the schema (Zod, Pydantic), one file owns the read.\n- **Fail loudly at boot** if required env is missing. Don't lazily fail at request time.\n- **Defaults are explicit.** No \"implicit fallback to localhost\" — write the fallback as a constant with a comment.\n\n## Magic values\n\n- **No magic numbers or strings.** Hoist to a named constant. `MAX_RETRIES`, `DEFAULT_TIMEOUT_MS`. Exception: 0, 1, -1 in obvious contexts.\n- **Enums for closed sets.** TypeScript `as const` + union type is usually cleaner than a TS `enum`.\n\n## Files and modules\n\n- **One default export per file.** Co-located helpers are fine but stay private.\n- **Filename matches the primary export.** `UserStore` lives in `UserStore.ts` (or `user-store.ts` — match the repo).\n- **No god files.** If a file exceeds ~400 lines, look for a missing extraction.\n- **Group by feature, not by type.** `src/users/{store,api,types,index}.ts` is better than `src/{stores,apis,types}/users.ts` past a small scale.\n\n## Whitespace and formatting\n\n- **Use the project's formatter.** Prettier, Black, gofmt, rustfmt — non-negotiable.\n- **No mixed tabs/spaces.** Project picks one; stick to it.\n- **One blank line between top-level declarations.** Two is shouting; zero is dense.\n\n## Comments about Claude / agents\n\nIf you (the AI agent) generated this code, the burden is higher: every helper you added must be used. Every parameter must be referenced. No \"just-in-case\" defensive code that you can't justify. See `~/.truegate/topics/anti-patterns.md` for the full list of things to avoid.\n\n## See also\n\n- [git-and-pr.md](./git-and-pr.md) — commit message conventions\n- [documentation.md](./documentation.md) — when comments aren't enough\n- [testing.md](./testing.md) — what every function should be tested for\n",
  "topics/database.md": "# Database\n\nSchema design, migrations, query patterns. Postgres-flavored — most rules apply to any relational DB.\n\n## Schema design\n\n- **Singular table names match the resource** — `user`, `order`, `invoice`. Some teams prefer plural — pick one in your project and stick to it.\n- **Primary keys**: prefer `uuid` (or [`uuidv7`](https://www.rfc-editor.org/rfc/rfc9562.html) for time-orderable) over auto-increment integers for any user-visible ID. Integers leak count + ordering.\n- **Foreign keys are NOT optional.** Always declare them. The DB enforces referential integrity; your app code will eventually have bugs.\n- **`NOT NULL` is the default.** Every column should be `NOT NULL` unless null carries semantic meaning. Three-valued logic catches you out.\n- **Timestamps**: `created_at`, `updated_at` (or `created_ts`/`updated_ts`) on every table. `timestamptz` (UTC), not `timestamp`.\n- **Soft-delete is contentious.** If you need it, add `deleted_at TIMESTAMPTZ NULL`. Query through views that filter `deleted_at IS NULL` to avoid app-side mistakes. Beware: unique constraints don't know about soft-delete.\n- **Enums vs lookup tables.** Postgres `ENUM` types are fast and type-safe but painful to alter. Lookup tables (`status_id` → `statuses(id, name)`) scale better when the list grows.\n- **JSONB sparingly.** Great for genuinely schemaless data (webhooks, audit logs, third-party blobs). Bad for fields you'll filter on heavily — indexes are awkward, statistics are weak.\n\n## Indexes\n\n- **Every column in a `WHERE`, `ORDER BY`, or `JOIN` gets an index** when the table is big enough to feel it.\n- **Composite indexes match the query order**: `(tenant_id, created_at DESC)` for \"show me a tenant's recent orders\" — leading column is the most selective filter.\n- **Partial indexes** for hot subsets: `CREATE INDEX ... WHERE deleted_at IS NULL`.\n- **`CREATE INDEX CONCURRENTLY`** in production — avoids long write locks.\n- **Index cost is non-zero** — every insert/update writes to every relevant index. Don't over-index small write-heavy tables.\n- **`EXPLAIN ANALYZE`** every slow query. Look for seq scans on big tables, sort spills to disk, nested loops with high row counts.\n\n## Constraints\n\n- **Check constraints** for invariants the app should never violate: `CHECK (price >= 0)`, `CHECK (start_date < end_date)`.\n- **Unique constraints** for natural keys: `UNIQUE (tenant_id, email)`.\n- **Exclusion constraints** for overlap rules (Postgres): no two bookings on the same room overlap.\n- **Triggers for derived data** are usually a smell. Compute in queries or materialize via a job. Triggers are invisible action-at-a-distance.\n\n## Transactions\n\n- **Keep transactions short.** Long-held transactions block writers, hold connection slots, and trip deadlock detection.\n- **Never hold a transaction across an external HTTP call.** This is the #1 cause of mystery slowdowns.\n- **Isolation level**: default is `READ COMMITTED` (Postgres, SQL Server). For invariants that span multiple rows, use `REPEATABLE READ` or `SERIALIZABLE` + retry on serialization failure.\n- **`SELECT ... FOR UPDATE`** when you read a row intending to update it in the same transaction. Avoids race conditions on the read-modify-write pattern.\n- **Optimistic concurrency** via a `version` column or ETag is often simpler than locking for low-contention workloads.\n\n## Connection pooling\n\n- **App-level pool**: small (5–20 per process). Postgres uses ~10 MB per backend; a 100-connection pool times 10 app instances = 1000 backend processes, which most Postgres servers can't sustain.\n- **Pooler in front**: PgBouncer in transaction-pooling mode for Postgres at scale. Beware of features that need session state (advisory locks, prepared statements, `SET` commands) — they break under transaction pooling.\n- **One pool per process**, not per request. Make it lazy-initialized and shut it down on `SIGTERM`.\n\n## Migrations — expand / contract\n\n**Every non-trivial change uses parallel-change.** Never destructive in a single migration.\n\n1. **Expand**: add new column / table. Keep old in place.\n2. **Backfill**: copy data, ideally in chunks, off-peak. Don't `UPDATE` a 100M-row table in one statement.\n3. **Dual-write**: app writes both old and new. Reads from new fall back to old.\n4. **Cut over reads**: app reads only new.\n5. **Stop writes to old.**\n6. **Contract**: drop old in a later, separate deploy.\n\nRules:\n\n- **One migration = one logical change.** No mixed DDL + data steps that can't be reapplied.\n- **Lock-free DDL**: `CREATE INDEX CONCURRENTLY`, `ADD CONSTRAINT ... NOT VALID` then `VALIDATE CONSTRAINT`, `ALTER TABLE ... SET NOT NULL` only after data is clean.\n- **Avoid full-table rewrites** on big tables. Tools: [gh-ost](https://github.com/github/gh-ost), [pt-online-schema-change](https://docs.percona.com/percona-toolkit/pt-online-schema-change.html), [pg_repack](https://github.com/reorg/pg_repack).\n- **Reversible by default.** Every migration has a tested `down`. Truly irreversible migrations (data loss) require an explicit backup gate.\n- **Migrations run before deploy** — new code must tolerate the old schema (expand), and old code must tolerate the new schema (until contract). This is what makes rollbacks safe.\n\nCatalog of dangerous migration patterns: [github.com/ankane/strong_migrations](https://github.com/ankane/strong_migrations).\n\n## Query patterns\n\n- **N+1 detection in tests**: assert query count per request; fail build on regression.\n- **Eager-load** known access patterns (`include` / `JOIN`).\n- **Avoid `SELECT *`** in production code. Name columns — surfaces schema drift, controls payload size.\n- **Pagination**: cursor-based for large datasets (see [backend.md](./backend.md)).\n- **Bulk operations**: `INSERT ... ON CONFLICT` (Postgres) / `INSERT ... ON DUPLICATE KEY` (MySQL) > N round-trips. Use `COPY` for thousands of rows.\n- **Aggregations on hot tables**: precompute in a `summary_*` table updated by a job, rather than recomputing on every read.\n\n## Row-Level Security (Postgres / Supabase)\n\nWhen you have multi-tenant or multi-user data with strict isolation:\n\n- Enable RLS on every tenant-scoped table: `ALTER TABLE x ENABLE ROW LEVEL SECURITY;`\n- Default deny: no policies → no rows visible.\n- Write policies per role: `CREATE POLICY ... ON x FOR SELECT USING (tenant_id = current_setting('app.tenant_id'))`.\n- App sets `SET LOCAL app.tenant_id = '...'` at the start of each request.\n- **Defense in depth** — app code still filters by tenant. RLS catches the bug; the app prevents the bug.\n\n## Backups\n\n- **Automated daily** at a minimum. Cloud-managed DBs do this; verify yours is on and tested.\n- **Point-in-time recovery (PITR)** for production — WAL archiving for Postgres.\n- **Test restores quarterly.** A backup you've never restored is not a backup.\n\n## Don't do\n\n- Soft-delete with no unique-constraint reckoning (`UNIQUE(email)` blows up when re-creating a deleted user)\n- Run schema migrations from app boot (race condition across replicas)\n- `DROP COLUMN` on production without first removing all references\n- Store secrets, passwords (use a hash + per-user salt — Argon2id or bcrypt cost 12+)\n- `varchar(N)` where N is arbitrary (use `text`; PG `varchar` and `text` are equally fast, `varchar(N)` is just a check constraint)\n- Computed columns / triggers that hide business logic in the DB\n\n## See also\n\n- [backend.md](./backend.md) — connection pooling, transactions in HTTP context\n- [security.md](./security.md) — authn/authz, secrets\n- [references/README.md](../references/README.md) — Postgres docs, \"Use the Index Luke\", Strong Migrations\n",
  "topics/documentation.md": "# Documentation\n\nWhat to write, where it lives, how to keep it from rotting.\n\n## README.md (every repo)\n\nThe README is the entry point. A new contributor should be able to:\n\n1. Understand what this project does (2-4 sentences)\n2. Install dependencies and run it locally\n3. Run the tests\n4. Know where to ask questions\n\nRequired sections, in order:\n\n```markdown\n# <Project name>\n\n> One-line description.\n\nA 2-4 sentence paragraph: what is this, who is it for, why does it exist.\n\n## Install\n\n…exact commands…\n\n## Develop\n\n…how to run the dev server, link to the architecture doc…\n\n## Test\n\n…the canonical test command…\n\n## Deploy\n\n…or link to a runbook…\n\n## License\n\n…\n```\n\nOptional (skip until needed): badges, contributors, sponsors, screenshots.\n\n**Don't:**\n\n- Don't put \"Coming soon\" sections. Just leave them out until you have content.\n- Don't auto-generate the entire README from a tool. The \"What is this?\" paragraph must be human-written.\n- Don't paste your roadmap into the README — it goes stale fast. Use issues / a project board.\n\n## Architecture docs\n\nFor anything non-trivial, a `docs/architecture/` directory:\n\n- `overview.md` — system diagram (Excalidraw or Mermaid), data-flow happy path.\n- One file per major subsystem.\n- Link from the README, link from each subsystem's own folder.\n\n**Keep diagrams in the repo as source files** (Mermaid in `.md`, Excalidraw `.excalidraw`). PNGs go stale; source diagrams update with the codebase.\n\n## Architecture Decision Records (ADRs)\n\nUse Michael Nygard's template — short, dated, immutable:\n\n```markdown\n# ADR-0007: Use Redis as the rate-limiter store\n\n**Status:** Accepted — 2026-03-15\n\n## Context\n\nWe need rate limits that survive process restarts and are shared across instances. Options considered: in-process Map (no), Postgres (works but row contention), Redis (token-bucket support, atomic INCR with EXPIRE).\n\n## Decision\n\nUse Redis with a Lua script implementing token bucket. Connection pool of 10 per app instance.\n\n## Consequences\n\n- We now require a Redis instance in every environment.\n- Failure mode: if Redis is down, requests pass through unthrottled. Document this in the runbook.\n- Existing token-bucket library `bottleneck` no longer needed; can be removed.\n```\n\nNumbered chronologically: `docs/adr/0001-foo.md`, `0002-bar.md`. Once accepted, don't edit — write a new ADR that supersedes it.\n\nReference: [Documenting Architecture Decisions (Nygard)](https://cognitect.com/blog/2011/11/15/documenting-architecture-decisions.html)\n\n## Comments in code\n\nThe full rule is in [code-style.md](./code-style.md). Summary:\n\n- Explain WHY, not WHAT.\n- Delete obvious comments.\n- TODOs need an owner and a date or they don't belong.\n- No commented-out code (git remembers).\n\n## Changelogs\n\nFor libraries, CLIs, or services with external consumers: `CHANGELOG.md` in [Keep A Changelog](https://keepachangelog.com) format.\n\n```markdown\n## [Unreleased]\n\n### Added\n\n### Changed\n\n### Fixed\n\n## [1.4.0] — 2026-02-12\n\n### Added\n\n- New `--strip-client-system` flag…\n```\n\nFor internal services with no external consumers: rely on git log + release tags. Don't write a changelog you won't maintain.\n\n## Inline tour-of-code\n\nFor genuinely complex modules, a `README.md` next to the source files explaining the moving parts:\n\n```\nsrc/auth/\n  README.md          # explains the OAuth dance, refresh strategy, token storage\n  session.ts\n  refresh.ts\n  ...\n```\n\nCo-location keeps it close to the code; PRs that change behavior should update the README.\n\n## API documentation\n\n- **REST APIs**: OpenAPI 3.1 spec, generated from code annotations (`zod-to-openapi`, FastAPI auto-docs) > hand-written.\n- **GraphQL APIs**: the schema IS the documentation. Add descriptions to types/fields.\n- **SDKs**: typed signatures > prose. Doc-comments visible in IDE hover.\n\n## Don't do\n\n- Leave \"TBD\" sections that linger forever\n- Document features that don't exist yet (the README is current state, not roadmap)\n- Use screenshots that include real user data\n- Lie. If the build is flaky, say so; if the deploy is manual, say so. Aspirational docs erode trust.\n- Maintain three versions of the same content in three places (README, wiki, Notion). Pick one canonical source.\n\n## See also\n\n- [git-and-pr.md](./git-and-pr.md) — PR descriptions, commit messages\n- [code-style.md](./code-style.md) — when comments are appropriate\n",
  "topics/frontend.md": "# Frontend (React 19 / Next.js 15 / current standards)\n\nResearch-backed reference for AI agents generating frontend code in 2025/26. Do not regress to pre-2024 patterns.\n\n## React 19 (stable Dec 2024)\n\n[react.dev/blog/2024/12/05/react-19](https://react.dev/blog/2024/12/05/react-19)\n\n**Idiomatic now:**\n\n- **Actions** — async functions passed to `<form action={fn}>` or `formAction`. Auto-manage pending/error state; forms auto-reset on success.\n- **`useActionState(action, initialState)`** — `[state, dispatch, isPending]`. Replaces the old `useFormState`.\n- **`useFormStatus()`** — read parent form's pending state from a child submit button without prop drilling.\n- **`useOptimistic(state)`** — optimistic UI during a mutation.\n- **`use(promise | context)`** — read a promise or context **during render**. Promises suspend; context can be read conditionally (unlike `useContext`).\n- **Server Components (RSC)** — render server-side, ship zero JS for server-only components. Default in App Router.\n- **`'use server'`** — marks a server-only async function exposed as an RPC endpoint, callable from Client Components.\n- **`'use client'`** — marks where a module (and its imports) become client-bundled. Put as high as makes sense, not on every leaf.\n- **`ref` as a prop** — no more `forwardRef` in new code (deprecated).\n- **Ref cleanup**: `ref={(node) => { ...; return () => {/* cleanup */} }}`.\n- **Document metadata** — render `<title>`, `<meta>`, `<link>` anywhere; React hoists into `<head>`.\n- **`<Context value={...}>`** as provider (no `.Provider`).\n\n**Deprecated / removed:** `forwardRef`, `useFormState`, `<Context.Provider>`, string refs, `ReactDOM.render`, `findDOMNode`.\n\n**Suspense:** wrap async boundaries; pair with RSC streaming. Avoid waterfalls — kick off promises in parents, pass down, `use()` in children.\n\n## Next.js 15 App Router\n\n[nextjs.org/blog/next-15](https://nextjs.org/blog/next-15) · [nextjs.org/docs/app](https://nextjs.org/docs/app)\n\n**Defaults that changed vs 14 (breaking):**\n\n- `fetch()` is **no longer cached by default**. Opt in: `fetch(url, { cache: 'force-cache' })` or `{ next: { revalidate: N } }`.\n- `GET` route handlers no longer cached by default. Opt in: `export const dynamic = 'force-static'`.\n- Client Router Cache `staleTime` defaults to 0 for page segments.\n- Request APIs are async: `await cookies()`, `await headers()`, `await params`, `await searchParams`, `await draftMode()`.\n- Node.js ≥ 18.18 required.\n\n**Data fetching:**\n\n- Default: fetch in Server Components. Co-locate `await fetch(...)` in the component that uses the data. No `getServerSideProps`/`getStaticProps`.\n- Mutations: **Server Actions** (`'use server'`) over route handlers. Call from `<form action={...}>` or imported in client components.\n- Route handlers (`route.ts`) for: webhooks, OAuth callbacks, public JSON APIs, non-form HTTP verbs.\n- Caching: `'force-cache'` (SSG-like), `'no-store'` (SSR-like), `{ next: { revalidate: 60 } }` (ISR), `{ next: { tags: ['x'] } }` + `revalidateTag('x')` for on-demand.\n\n**File conventions:**\n\n| File                   | Purpose                                         |\n| ---------------------- | ----------------------------------------------- |\n| `app/layout.tsx`       | Root layout (must render `<html>` and `<body>`) |\n| `app/page.tsx`         | Route's UI                                      |\n| `app/loading.tsx`      | Suspense fallback for segment                   |\n| `app/error.tsx`        | Error boundary (Client Component)               |\n| `app/not-found.tsx`    | Rendered by `notFound()` or unmatched routes    |\n| `app/route.ts`         | HTTP route handler                              |\n| `app/template.tsx`     | Like layout but remounts on navigation          |\n| `app/global-error.tsx` | Catches errors in the root layout               |\n| `app/(group)/`         | Route group, no URL segment                     |\n| `app/[param]/` etc     | Dynamic segments                                |\n\n**Metadata:** `export const metadata: Metadata = {...}` or `export async function generateMetadata({ params })`. File-based: `opengraph-image.tsx`, `icon.tsx`, `sitemap.ts`, `robots.ts`, `manifest.ts`.\n\n**Other 15-era:** `next.config.ts` with types, `<Form>` from `next/form`, `instrumentation.ts` + `onRequestError` hook, `unstable_after()`, Turbopack dev stable, React Compiler available.\n\n## Core Web Vitals (current)\n\nSee [topics/performance.md](./performance.md) for full table + targets. Thresholds at p75: LCP ≤ 2.5s, **INP ≤ 200ms** (replaced FID on 12 Mar 2024), CLS ≤ 0.1.\n\n## WCAG 2.2 AA (Oct 2023 W3C Rec)\n\nFull details in [topics/accessibility.md](./accessibility.md). Frontend-specific note: 24×24 px target size, focus-not-obscured, dragging alternatives, accessible auth (no transcription-only flows).\n\n## CSS / styling — prefer now\n\n- **Tailwind CSS v4** ([blog/tailwindcss-v4](https://tailwindcss.com/blog/tailwindcss-v4)) — CSS-first config via `@theme { --color-... }`. No `tailwind.config.js`. Single `@import \"tailwindcss\";`. Container queries (`@container`), 3D transforms, `not-*`, `@starting-style`.\n- **CSS custom properties** for design tokens — readable from JS via `getComputedStyle`.\n- **`:has()` selector** (Baseline) — parent/sibling conditional styling instead of JS class toggling.\n- **Container queries** (`@container`, `cqw`/`cqi` units) — prefer over viewport media queries for component-level responsiveness.\n- **Native CSS nesting** (Baseline).\n- **`color-mix()`, `oklch()`, relative color** — for token systems.\n\n**Discouraged:** styled-components (maintenance mode Mar 2025), Emotion / runtime CSS-in-JS (hostile to RSC), Sass for new code, manual breakpoints when container queries fit.\n\n## Images\n\n[nextjs.org/docs/app/api-reference/components/image](https://nextjs.org/docs/app/api-reference/components/image)\n\n- Use `next/image` — responsive `srcset`, lazy-loading default, CLS-safe via required `width`/`height` or `fill`.\n- `priority` on the LCP image (adds `fetchpriority=\"high\"` + preload).\n- `next/image` serves AVIF then WebP by default.\n- `sizes` prop is **mandatory** with `fill` or non-fixed layouts.\n- `alt` always (empty `alt=\"\"` for purely decorative).\n- Don't use `next/image` for icons/SVG — inline SVG (smaller, themable via `currentColor`).\n\n## State management — decision order\n\n1. **Local state** (`useState`, `useReducer`) — default for component-scoped state.\n2. **URL state** (search params / route params) — for shareable, bookmarkable, server-renderable state (filters, tabs, pagination).\n3. **Server state cache** — TanStack Query / SWR or RSC + Server Actions. **Most \"global state\" is server state.**\n4. **React Context** — low-frequency shared values (theme, auth user). Not for high-frequency updates.\n5. **Client store (Zustand / Jotai)** — only for genuinely client-side, cross-tree, high-frequency state (canvas/editor, multi-step wizards).\n\n**Overused:** Redux for new apps, Context for everything (cascade re-renders), putting server data in Zustand (you'll reinvent caching).\n\n## Forms\n\n- **React Hook Form** for complex client-side forms — uncontrolled by default, cheap re-renders.\n- **Server Actions for submission** — `<form action={serverAction}>` + `useActionState` + `useFormStatus`.\n- **Zod for validation** — one schema, validate on server AND client via `@hookform/resolvers/zod`.\n- **Progressive enhancement** — works without JS via Server Actions.\n- **Accessible errors**: `aria-describedby` linking input to error; `aria-invalid=\"true\"`; `aria-live=\"polite\"` region OR inline immediately after; `<label htmlFor>` always; `<fieldset><legend>` for related fields; don't disable submit on invalid.\n\n## Testing frontend\n\n- **Vitest > Jest** for new projects — native ESM, faster, same API.\n- **Testing Library** — query by role/name first (`getByRole('button', { name: /save/i })`), then label, then text. `data-testid` is a fallback smell.\n- **`user-event` v14+** (`await userEvent.click(...)`), not `fireEvent`.\n- **Playwright for E2E** — cross-browser, auto-waits, trace viewer. Prefer over Cypress for new work.\n- **MSW** for network mocks.\n- **axe-core / @axe-core/playwright** for automated a11y in CI.\n\n**Don't test:** implementation details (internal state, private methods, class names), library code you don't own, large DOM snapshots, styling in unit tests (use visual regression).\n\n## Don't do\n\n- `dangerouslySetInnerHTML` without DOMPurify AND a documented reason\n- `useEffect` for data fetching (use Server Components or a query lib)\n- Client component when a server component would do\n- Re-exporting `'use client'` modules from server-only files (creates accidental client boundaries)\n- 3rd-party scripts loaded synchronously in `<head>`\n- Lazy-loading the LCP image\n- New apps on Pages Router (App Router is default since Next 13.4)\n\n## See also\n\n- [components/forms.md](../components/forms.md)\n- [components/data-display.md](../components/data-display.md) (tables, lists, empty states)\n- [patterns/data-fetching.md](../patterns/data-fetching.md)\n- [accessibility.md](./accessibility.md)\n- [performance.md](./performance.md)\n",
  "topics/git-and-pr.md": "# Git, commits, and pull requests\n\n## Branch naming\n\n- **Use prefixes** for type, then a short topic: `feat/oauth-refresh`, `fix/leaky-fd`, `chore/bump-react`, `docs/installation-guide`.\n- **No spaces, no special chars** beyond `-` and `/`.\n- **Tie to an issue if applicable**: `fix/auth-401-issue-1234`.\n- **Delete branches after merge.** GitHub does this automatically — enable the setting.\n\n## Commit messages — Conventional Commits\n\nFollow [conventionalcommits.org](https://www.conventionalcommits.org/) format:\n\n```\n<type>(<optional scope>): <description>\n\n<optional body>\n\n<optional footer>\n```\n\n**Types**: `feat`, `fix`, `chore`, `docs`, `refactor`, `test`, `perf`, `style`, `build`, `ci`, `revert`.\n\n**Examples**:\n\n```\nfeat(auth): add refresh-token rotation\n\nfix(api): handle empty body on POST /webhooks\n\nchore(deps): bump zod from 3.22 to 3.23\n\nrefactor(governance): split rules.yaml loader into smaller files\n```\n\n**Breaking changes** get a `!` after type and a footer:\n\n```\nfeat(api)!: rename /v1/users to /v2/users\n\nBREAKING CHANGE: clients on v1 must migrate by 2026-Q3. v1 will be removed.\n```\n\nThis format powers automated changelog generation, semver releases, and PR auto-labelling.\n\n## Commit hygiene\n\n- **Atomic commits.** One logical change per commit. \"WIP\" commits are fine on a branch but squash before merging if your team requires.\n- **Imperative mood.** \"add login\" not \"added login\" / \"adds login\".\n- **First line ≤ 72 characters.** Body wraps at 72 too. Most tools assume this.\n- **Reference issues** in the footer: `Refs #123` or `Closes #123` (the latter auto-closes the issue when merged).\n- **Don't lie in commit messages.** \"fix bug\" tells future-you nothing. Be specific about what behavior changed.\n\n## What goes in a PR description\n\nThree sections, always:\n\n```markdown\n## Summary\n\nTwo or three sentences. What problem, what solution, what changed.\n\n## Test plan\n\n- [ ] Existing tests pass\n- [ ] Added a regression test for the bug at `path/to/test`\n- [ ] Manual verification: triggered the original error, confirmed fix\n\n## Risk / rollback\n\n(Only if non-trivial.) What could break, how to roll back, who to page.\n```\n\nFor a 1-line typo fix, the test plan is \"n/a — typo only\". For anything bigger, fill it out honestly.\n\n## PR sizing\n\n- **Aim for under 400 lines changed** (Google's research says review quality falls off a cliff above this).\n- **If it's too big**, split it. Logical seams: refactor first (no behavior change), then add the new behavior.\n- **Stacked PRs** for changes that need to ship together but reviewers should see incrementally.\n\n## Code review etiquette\n\n- **Author**: don't ship a PR you haven't actually read end-to-end yourself.\n- **Reviewer**: comment on the code, not the person. \"This could be simpler — what about X?\" not \"This is bad\".\n- **Block on real problems** (security, correctness, data integrity). Style nits are nits — leave them as a non-blocking comment.\n- **Ask if you don't understand** before approving. An \"LGTM\" you didn't really read is a future debugging session for someone else.\n\n## Merge strategies\n\n| Strategy             | When                                                                                           |\n| -------------------- | ---------------------------------------------------------------------------------------------- |\n| **Squash and merge** | Default for small features and fixes. Keeps `main` linear, one commit per PR.                  |\n| **Merge commit**     | When the branch's individual commits tell a useful story (rare).                               |\n| **Rebase and merge** | When you want each commit on `main` but no merge commit. Requires clean commits on the branch. |\n\nPick one as the team default; enforce in GitHub branch protection.\n\n## Tags and releases\n\n- **Semver** for libraries: `MAJOR.MINOR.PATCH`. Breaking change → MAJOR bump.\n- **CalVer** for products with rolling releases: `YYYY.MM` or `YYYY.MM.PATCH`.\n- **Tag in git, release in GitHub.** Release notes assembled from PR titles + manually-curated highlights.\n\n## Don't do\n\n- `--force-push` to `main` or any shared branch — ever, without team coordination\n- `--no-verify` to skip pre-commit hooks (the hooks exist for a reason — fix the underlying issue)\n- Commit `node_modules/`, `.env`, build artifacts, IDE folders (`.idea/`, `.vscode/` is debatable per repo)\n- Commit secrets, even briefly. If you ever do, rotate the secret AND scrub git history (`git filter-repo`)\n- `git rebase` published branches that others are working on\n- Land a PR you haven't read end-to-end yourself\n\n## See also\n\n- [documentation.md](./documentation.md) — when changes warrant a CHANGELOG entry\n- [testing.md](./testing.md) — the test plan section of your PR\n",
  "topics/performance.md": "# Performance\n\nPerformance budgets that matter, current (2025/2026) targets, and the cheapest interventions per dollar of effort.\n\n## Frontend — Core Web Vitals (Google, current thresholds)\n\nThree metrics define \"good\" performance for real users:\n\n| Metric                              | Good     | Needs improvement | Poor     |\n| ----------------------------------- | -------- | ----------------- | -------- |\n| **LCP** (Largest Contentful Paint)  | ≤ 2.5 s  | ≤ 4.0 s           | > 4.0 s  |\n| **INP** (Interaction to Next Paint) | ≤ 200 ms | ≤ 500 ms          | > 500 ms |\n| **CLS** (Cumulative Layout Shift)   | ≤ 0.1    | ≤ 0.25            | > 0.25   |\n\nSource: [web.dev/articles/vitals](https://web.dev/articles/vitals)\n\n**INP replaced FID in March 2024** ([web.dev/blog/inp-cwv-launch](https://web.dev/blog/inp-cwv-launch)). FID measured first input only; INP measures all interactions across the page lifetime.\n\nMeasure at the 75th percentile across page views, not the average.\n\n## What moves each metric\n\n### LCP\n\nThe single biggest lever is **the LCP image** — usually a hero image or above-the-fold banner.\n\n- Serve it as a modern format (AVIF, WebP fallback to JPEG).\n- Set explicit `width`/`height` so the browser reserves space (also helps CLS).\n- Use `loading=\"eager\"` and `fetchpriority=\"high\"` on the LCP image. Don't lazy-load it.\n- `<link rel=\"preload\">` for the LCP image if it's a CSS background.\n- Self-host fonts; preload the LCP font: `<link rel=\"preload\" as=\"font\" crossorigin>`.\n\n### INP\n\nThe single biggest lever is **less main-thread JavaScript work**.\n\n- Defer/lazy-load components that aren't visible.\n- Split large event handlers; use `requestIdleCallback` / `scheduler.yield()` for non-critical work.\n- Avoid sync layout reads inside scroll/input handlers (force reflow).\n- Server components / RSC reduce client JS substantially when used correctly.\n- Avoid third-party scripts (analytics, chat widgets) in the critical path — load on `requestIdleCallback`.\n\n### CLS\n\nThe single biggest lever is **explicit dimensions on everything that loads asynchronously**.\n\n- `width`/`height` on every `<img>` and `<video>`.\n- Reserve space for ads/embeds with a fixed-height container.\n- Avoid inserting content above existing content (sticky banners that push content down).\n- Font-loading: use `font-display: optional` or `swap` with metrics-matched fallback fonts to avoid late shifts.\n\n## Frontend — JavaScript budget\n\n- **Initial JS payload**: < 150 KB gzip is a reasonable target for marketing pages. Apps can be larger but watch the trend.\n- **Per-route JS**: < 50 KB delta per Next.js page.\n- **Use route-level code splitting.** App Router does this by default.\n- **`next/dynamic`** for components used only after user interaction (modals, complex editors).\n\n## Frontend — image rules\n\n- **Always use `next/image` or equivalent.** Hand-rolled `<img>` skips format optimization, lazy-loading, srcSet generation.\n- **Above the fold → priority. Below the fold → lazy.**\n- **Modern formats**: AVIF > WebP > JPEG. `next/image` handles this automatically.\n- **Responsive sizes**: `sizes=\"(max-width: 768px) 100vw, 50vw\"` so mobile doesn't download desktop assets.\n\n## Backend — API latency budgets\n\nSoft targets per endpoint class (p99):\n\n| Class                          | Target p99 |\n| ------------------------------ | ---------- |\n| Read, cached                   | < 50 ms    |\n| Read, hot DB                   | < 200 ms   |\n| Write, simple                  | < 500 ms   |\n| Write, complex (transactional) | < 1500 ms  |\n\nPast these, you ARE the bottleneck for whatever calls you.\n\n## Backend — the cheapest wins\n\n1. **Caching with proper invalidation.** Redis or CDN. Even 60 s TTL on a slow endpoint changes everything.\n2. **Avoid N+1.** ORMs hide these — use the ORM's \"include\"/\"with\" or write explicit JOINs. Detect in dev with `pg_stat_statements` or your ORM's query log.\n3. **Indexes.** Any column in a `WHERE`, `ORDER BY`, or `JOIN` needs an index when the table grows.\n4. **Connection pooling.** Don't open a new DB connection per request. Pool size: `(cores × 2 + spindle_count)` is the old rule; for cloud DBs, start at 5-20 per app instance.\n5. **Compression.** gzip or Brotli on responses > 1 KB. Most gateways do this — check yours is configured.\n6. **HTTP/2 (or HTTP/3).** Multiplexing alone helps any page that loads multiple resources from the same origin.\n\n## Detection\n\n- **Lighthouse CI** in pull requests → catches regressions before merge.\n- **Real-user monitoring (RUM)** — Vercel Analytics, Cloudflare Web Analytics, SpeedCurve, Sentry Performance. Lab numbers ≠ field numbers.\n- **OpenTelemetry traces** for backend latency. Service-level SLOs, alert on burn rate.\n\n## Don't do\n\n- Lazy-load the LCP image\n- Hide layout-shifting content with `opacity: 0` (it still triggers CLS at first paint)\n- Add 200 KB of polyfills for browsers your users don't run (drop IE11 support already)\n- Ship a `node_modules` dep just to format a date (Intl.DateTimeFormat is built in)\n- Put analytics scripts in `<head>` synchronously\n- Run a database query inside a render loop (\"just this once\" becomes 10k iterations)\n\n## See also\n\n- [frontend.md](./frontend.md) — React-specific perf\n- [backend.md](./backend.md) — caching, pooling specifics\n- [database.md](./database.md) — indexes, query plans\n- [references/README.md](../references/README.md) — web.dev, Lighthouse docs\n",
  "topics/security.md": "# Security\n\nNon-negotiable across every project. trueGate enforces some of this in `rules.yaml` (built-in patterns + your operator-wide rules). The rest is for you to follow.\n\n## Secrets and credentials\n\n- **Never embed** API keys, tokens, passwords, or connection strings in code, comments, or config files committed to git.\n- **Use environment variables OR a secret manager.** Cloud providers' KMS (AWS Secrets Manager, GCP Secret Manager, Vault) is the canonical answer for production.\n- **Rotate on suspicion.** If a key was logged, screenshared, pasted into chat, or shipped to a third party — rotate. Don't wait for evidence of misuse.\n- **Never log secrets.** Not even truncated. Use redaction middleware on your logger.\n- **Never put secrets in URLs.** They end up in proxy logs, browser history, CDN caches.\n- **`.env` files**: `.env.local` (gitignored, dev only). `.env.example` (committed, no real values). Never commit `.env` itself.\n\nReference: OWASP Cheat Sheet — [Secrets Management](https://cheatsheetseries.owasp.org/cheatsheets/Secrets_Management_Cheat_Sheet.html)\n\n## Authentication\n\n- **Never roll your own.** Use Auth0, Clerk, NextAuth, Supabase Auth, Cognito, or equivalent. Crypto bugs are silent and irreversible.\n- **OAuth 2.1 with PKCE** for any flow involving a third party. Never use Implicit Flow (deprecated).\n- **JWT or opaque tokens** for sessions. JWTs are stateless but irrevocable until expiry; opaque tokens require a session store but support instant logout. Pick consciously.\n- **Short access-token lifetimes (15-60 min)** + refresh tokens. Don't issue 30-day access tokens.\n\n## Authorization\n\n- **At the boundary, always.** Every HTTP route, every GraphQL resolver, every server action checks who the caller is and what they're allowed to do — BEFORE touching data.\n- **Never return early \"for performance\" before an authz check.** That's how privilege-escalation bugs ship.\n- **Default deny.** New endpoints should require auth by default; add an explicit allow-anonymous decorator if it's intentional.\n- **Row-level security (RLS) for databases that support it** (Postgres, Supabase). Defense-in-depth so an app bug can't leak another tenant's data.\n- **Audit who can do what.** Roles, permissions, policies — write them down. See OWASP API Top 10 #1 (Broken Object Level Authorization, BOLA).\n\n## Input validation\n\n- **Treat all external input as untrusted.** URL params, headers, request bodies, file uploads, query results from third-party APIs, environment values from third parties.\n- **Validate at the boundary** with a schema library (Zod, Joi, Pydantic, Marshmallow). Reject bad input with 400, not 500.\n- **Never trust client-side validation.** It's UX, not security.\n- **Length limits on every string field.** Email max 254. Comment max ~10k. URL max 2k. Otherwise you ship DoS vectors.\n\n## Injection\n\n- **Always use parameterized queries.** Never string-concatenate SQL. ORMs handle this; raw drivers like `pg`/`mysql2` support `$1`-style placeholders.\n- **Beware ORM raw escapes.** `prisma.$queryRaw` with template literals is safe; `$queryRawUnsafe` is not.\n- **Shell injection**: never pass user input as an argument to `exec()`, `system()`, `subprocess.Popen(... shell=True)`. Use the array form with explicit args.\n- **HTML injection (XSS)**: framework's built-in escaping is correct; bypassing it (React's `dangerouslySetInnerHTML`, Vue's `v-html`) requires sanitization (DOMPurify) AND a reason.\n\n## TLS\n\n- **TLS 1.2 minimum, 1.3 preferred.** Disable 1.0 and 1.1 at the load balancer.\n- **Never disable cert verification.** `rejectUnauthorized: false`, `verify=False`, `curl -k` are red flags. trueGate blocks these patterns automatically.\n- **HSTS for production sites.** `Strict-Transport-Security: max-age=63072000; includeSubDomains; preload`.\n\n## CSRF + cookies\n\n- **`SameSite=Lax`** for session cookies by default. `Strict` for high-value cookies (auth, payment). `None` only with `Secure` AND a real cross-site reason.\n- **`HttpOnly`** for session cookies. Always.\n- **`Secure`** for any cookie touched by an authenticated user.\n- **CSRF tokens** for state-changing operations served over cookies. Bearer tokens in headers are already CSRF-immune.\n\n## CORS\n\n- **Be specific about Origin.** `Access-Control-Allow-Origin: *` is fine for fully public read-only APIs. Otherwise, an allowlist.\n- **Never `*` with credentials.** Browsers won't honor it anyway, but the misconfiguration is a smell.\n\n## Logging\n\n- **Structured (JSON, key=value), with redaction.** Body and header redaction at the logger, not at every call site.\n- **Don't log full request bodies in production.** Just the path, method, status, latency, user ID (not email/PII), and a sampled subset of headers.\n\n## Dependency hygiene\n\n- **`npm audit` / `pip-audit` / `cargo audit`** in CI. Block deploys on `high` or `critical` findings.\n- **Pin dependencies** (lockfile committed).\n- **Watch the supply chain.** Don't add a brand-new package with one maintainer to production code without reading it.\n\n## OWASP Top 10 (current: 2021)\n\nThe big ten — in priority order:\n\n1. Broken Access Control\n2. Cryptographic Failures\n3. Injection\n4. Insecure Design\n5. Security Misconfiguration\n6. Vulnerable and Outdated Components\n7. Identification and Authentication Failures\n8. Software and Data Integrity Failures\n9. Security Logging and Monitoring Failures\n10. Server-Side Request Forgery (SSRF)\n\nFull list: [owasp.org/Top10](https://owasp.org/www-project-top-ten/)\n\n## See also\n\n- [backend.md](./backend.md) — API-level security patterns\n- [frontend.md](./frontend.md) — XSS, CSP, secure cookies in practice\n- `~/.truegate/references/README.md` — links to OWASP, MDN security headers, RFC 9580 (OAuth 2.1)\n",
  "topics/testing.md": "# Testing\n\nWhat to test, what NOT to test, current tooling (2025/26).\n\n## Testing Trophy vs Pyramid\n\n- **Pyramid** (Cohn, 2009): many unit, fewer integration, very few E2E. Optimized for slow/expensive E2E.\n- **Trophy** (Kent C. Dodds, 2018): static → unit → **integration (largest tier)** → E2E. Rationale: most bugs live at the seams between units, not inside them. Modern integration tests are fast enough (jsdom, in-memory DBs, MSW, testcontainers) to invert the pyramid.\n- **Current consensus:** Trophy for application code; Pyramid still fits library code with rich pure-functional cores.\n- **Heuristic:** write the test at the highest level you can afford to keep fast and stable.\n\nSources: [kentcdodds.com/blog/the-testing-trophy](https://kentcdodds.com/blog/the-testing-trophy-and-testing-classifications) · [kentcdodds.com/blog/write-tests](https://kentcdodds.com/blog/write-tests).\n\n## What to test\n\n**Yes:**\n\n- Public API contracts (function signatures, HTTP routes, message shapes)\n- Observable behavior\n- Happy path\n- Interesting edges (empty, max, boundary, unicode, timezone)\n- Error paths (invalid input, dependency failure, partial failure)\n\n**No:**\n\n- Implementation details — private methods, internal field names, call counts to internal collaborators, branch coverage for its own sake\n- Library code you don't own\n- Styling in unit tests (use visual regression)\n\n**Rule:** if a refactor that preserves observable behavior breaks the test, the test was wrong.\n\nSources: [kentbeck.github.io/TestDesiderata](https://kentbeck.github.io/TestDesiderata/) · [martinfowler.com/bliki/UnitTest](https://martinfowler.com/bliki/UnitTest.html).\n\n## AAA / Given-When-Then\n\nTwo conventions for the same shape:\n\n- **AAA:** Arrange → Act (ONE call) → Assert.\n- **Given-When-Then** (Dan North, 2006): same structure, BDD prose framing.\n\nDiscipline: blank-line separated blocks; one logical assertion per test (multiple `expect` calls fine if they describe one behavior); name the test after the _behavior_, not the method.\n\nSource: [dannorth.net/introducing-bdd](https://dannorth.net/introducing-bdd/).\n\n## Test doubles (Meszaros / Fowler)\n\n- **Dummy** — passed but never used (a filler arg).\n- **Stub** — returns canned answers. State-based.\n- **Fake** — working implementation, unsuited for production (in-memory repository, SQLite for Postgres). **High fidelity, fast — prefer this.**\n- **Spy** — a stub that also records how it was called.\n- **Mock** — pre-programmed with expectations; fails the test if calls don't match. Interaction-based (\"behavior verification\").\n\n**Prefer fakes > stubs > mocks.** Mocks couple tests to interaction sequences; refactor breaks them.\n\nSource: [martinfowler.com/articles/mocksArentStubs](https://martinfowler.com/articles/mocksArentStubs.html).\n\n## TDD\n\nRed → green → refactor. Kent Beck, 2002.\n\n- **Pays off:** clear contracts (parsers, serializers, pricing engines, validators); regression-locking a bug fix (write failing test first); algorithm-heavy code; refactoring under a safety net.\n- **Doesn't:** exploratory UI work where the design is the question; prototypes you'll throw away; integrations with poorly-documented third parties (spike first, then tests).\n\nHonest take: strict TDD is a discipline, not a religion. \"Test-first for new behavior, refactor-after for exploration\" is what most senior engineers actually do.\n\nSource: Beck, _Test-Driven Development: By Example_ (2002).\n\n## Integration test boundaries\n\nService-level: spin up the app and exercise its real entry points (HTTP routes, CLI commands, queue consumers).\n\n- **Mock:** external HTTP APIs (MSW, nock, undici MockAgent); third-party SDKs you don't own; clocks (`vi.useFakeTimers`); randomness; outbound email/SMS.\n- **Do NOT mock:** your own database, your own filesystem, your own message broker. Use **testcontainers** (real Postgres/Redis/Kafka in Docker) or ephemeral local instances. Mocking your own infra produces tests that pass while production breaks.\n\nSources: [testcontainers.com](https://testcontainers.com/) · [martinfowler.com/bliki/IntegrationTest](https://martinfowler.com/bliki/IntegrationTest.html) · [mswjs.io](https://mswjs.io/).\n\n## E2E\n\n**Playwright is the default in 2025/26.** Cypress is fading: slower CI, single-tab limitation, weaker multi-origin and mobile story.\n\n- **Do E2E:** critical user flows only — signup, login, checkout, primary \"job to be done.\" Smoke after deploy.\n- **Don't E2E:** every input field, every validation message, every conditional render — those belong in component/integration tests.\n- **Budget:** 10–30 E2E tests for most apps. If you have 500, you have a maintenance disaster, not a safety net.\n\nSource: [playwright.dev](https://playwright.dev/).\n\n## Snapshot tests\n\n- **Help:** golden-file fixtures with stable, reviewable output — generated SQL, compiled config, CLI help text, serialized AST. Diffs are meaningful; updates are intentional.\n- **Noise:** whole React component trees, large JSON dumps. Engineers reflexively run `--update-snapshots` and the test stops catching anything.\n- **Rule:** snapshot only when the snapshot itself is small enough to read in code review.\n\nSource: [kentcdodds.com/blog/effective-snapshot-testing](https://kentcdodds.com/blog/effective-snapshot-testing).\n\n## Fixtures and factories\n\n**Builders > static fixtures.**\n\n- Static JSON fixtures rot: every new required field forces N file edits; tests become coupled to irrelevant data.\n- Builder/factory pattern (`aUser().withEmail(\"x@y\").withRole(\"admin\").build()`): each test states only fields that matter; defaults live in one place; adding a required field is a one-line change.\n- Libraries: `@faker-js/faker` for realistic values; `fishery` (TS) or `factory_boy` (Python) for typed factories.\n\nSource: [Test Data Builders — Nat Pryce](http://www.natpryce.com/articles/000714.html).\n\n## Flaky tests — the four root causes\n\nIn order of frequency:\n\n1. **Time** — `Date.now()`, timezones, `setTimeout`, race conditions on async code. Fix: inject a clock; `vi.useFakeTimers`; `await` everything; no `sleep(100)` as synchronization.\n2. **Test ordering / shared state** — a test mutates a global, the DB, env vars, or module state and only fails after another test. Fix: isolate per-test (fresh DB schema or transaction rollback; reset modules; no module-level mutable state).\n3. **Real network** — calling real third-party APIs. Fix: mock at the HTTP boundary (MSW, MockAgent). If you must hit it, gate behind a tag and exclude from default runs.\n4. **Concurrency / resource contention** — parallel tests fighting for the same port, file, or DB row. Fix: per-worker resources (`vi.workerId`-scoped DB, ephemeral ports).\n\n**How to find them:** re-run failed tests 50× in CI; bisect with `--shuffle` / `--random-order`; track flake rate per test; **quarantine offenders rather than retry-paper**. Never add a \"retry 3 times\" rule as the fix — it hides the cause and erodes trust in the suite.\n\nSource: [testing.googleblog.com — Flaky Tests at Google](https://testing.googleblog.com/2016/05/flaky-tests-at-google-and-how-we.html).\n\n## Don't do\n\n- Mock your own database\n- Snapshot whole component trees\n- Test private methods directly\n- \"Retry 3 times\" as a flake fix\n- Skip tests with `.skip` and leave them indefinitely (mark with TODO + date + owner)\n- Tests that depend on test execution order\n- Reach for E2E when an integration test would do\n- Manual smoke tests as a substitute for automated ones\n\n## See also\n\n- [architecture.md](./architecture.md) — testable architectures\n- [git-and-pr.md](./git-and-pr.md) — the test plan section of your PR\n- [references/README.md](../references/README.md) — Kent C. Dodds, Fowler on testing\n",
};
